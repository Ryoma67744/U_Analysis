# =============================================================================
# MSI Analysis Application - Docker Compose
# 開発・テスト用構成
# =============================================================================

services:
  msi-app:
    build: .
    container_name: msi-analysis-app
    ports:
      - "${APP_PORT:-3838}:3838"
    environment:
      - R_HOME=/usr/lib/R
      - APP_HOST=0.0.0.0
      - APP_PORT=3838
      - APP_PASSWORD=${APP_PASSWORD:-}
      - SHARE_BASE_URL=${SHARE_BASE_URL:-}
    volumes:
      # 永続化データ (Data/ 配下)
      - msi-sessions:/app/Data/sessions
      - msi-projects:/app/Data/projects
      - msi-presets:/app/Data/presets
      - msi-shares:/app/Data/shares
      - msi-cache:/app/Data/cache
      # 解析データ（ユーザーがアップロード）
      - msi-desi-data:/app/Data/DESI/Data
      - msi-tims-data:/app/Data/TIMS/Data
      # 解析出力
      - msi-output:/app/Data/output
    restart: unless-stopped
    mem_limit: 12g

volumes:
  msi-sessions:
  msi-projects:
  msi-presets:
  msi-shares:
  msi-cache:
  msi-desi-data:
  msi-tims-data:
  msi-output:
