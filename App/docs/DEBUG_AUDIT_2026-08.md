# U_Analysis デバッグ総点検報告書（2026-08）

対象アプリ：U_Analysis（TIMS・DESI の質量分析イメージングを UMAP で解析・探索・共有する研究室向け Web アプリ。R 解析スクリプト群＋Python/Dash UI。調査時点 ver56.3）
作成日：2026-08-15
位置づけ：「① アプリ実行時にエラーがないか ② 登録されているボタンなどが意図したとおりに実行されるか ③ R スクリプト・解析内容が科学的に正確か」の 3 点について、**全スクリプトを対象に**行った総点検の記録。
方針メモ：本報告書は調査結果のまとめである。**調査そのものはアプリのコード・解析スクリプトを一切変更せずに行った**（複製環境で実行）。修正は §7 の区分に従い項目ごとに着手し、ver56.4 では「数値が変わらない機械的修正」2 件のみを実施した。**解析結果の数値が変わる修正は一切行っていない**（§7.2・§7.3 として承認待ち）。プログラミング用語は最小限にし、「利用者から見て何が起きるか」と「研究上の意味」を中心に説明する。

---

## 0. この文書の読み方

### 重大度

| 記号 | 意味 |
|---|---|
| **S1** | **科学的に誤った結果が出る**／データが壊れる／アプリが起動しない |
| **S2** | 利用者に見える誤動作（押しても効かない・違う結果になる・変更が反映されない） |
| **S3** | 条件が揃ったときだけ起きる、または潜在的な危険 |
| **S4** | 軽微（表示・記録の不整合、無駄な処理、将来の地雷） |

### 確証度 — 「どこまで確かめたか」を全項目に明示する

| 記号 | 意味 |
|---|---|
| **[実行確認]** | 実際にアプリを操作した、関数を呼んだ、スクリプトを走らせて**再現した** |
| **[コード確認]** | コードを読んで論理的に確定した（実行はしていない） |
| **[要データ検証]** | 実データがないと最終確認できない |

**本報告書の指摘 79 件のうち 78 件が [実行確認] である。** 推測に基づく指摘は原則として載せていない。

### 判定

すべての指摘は、それを見つけた監査担当とは**別の検証官**が「この主張は間違っている」と示すことを目標に再検証した（反証審査）。その結果を次の 3 つに分けている。

- **確定**（UPHELD）：反証を試みたが崩せなかった。
- **条件付き**（WEAKENED）：機構は正しいが、影響範囲や重大度が過大だったので訂正した。
- **誤検出**（OVERTURNED）：反証が成立した（4 件）。**隠さず §4.4 / §5.6 に公開する**（監査が「疑いをすべて事実にしていない」ことの証明として残す）。

### 修正区分

- **機械的修正**：直しても解析結果の数値は変わらない。設計判断も不要。
- **要科学的判断**：直すと**過去の解析結果と数値が変わる**。研究上の判断が必要。
- **要設計判断**：仕様をどうするかの選択が必要。

---

## 1. エグゼクティブサマリー

### 結論を一言で

**アプリは「落ちない」。しかし「黙って言うことを聞かない」箇所が多数ある。**

サーバ側のエラー（HTTP 500、未捕捉例外、コールバックの実行時エラー）は探査中**ゼロ件**、自動テスト 1,756 件・E2E 19 件も全て緑で、R スクリプト 51 本すべてが構文上正しい。この意味で①「実行時のエラー」は健全である。

問題はそこではなく、**「設定したのに効かない」「押したのに何も起きない」「変更したのに反映されない」が、エラーを一切出さずに起きる**点に集中している。これはご指摘の②の症状そのものであり、その具体的な原因を 43 件の S2 として特定した。さらに③については、**解析結果の数値が実際に誤る問題を 5 件（S1）**確認した。

### 数字で見る点検範囲

| 対象 | 範囲 | 結果 |
|---|---|---|
| Dash コールバック | **353 件**（＋ブラウザ側 20 件）を全数 | 稼働中アプリの登録グラフ 381 件と全件突合 |
| ボタン | **214 個**を全数（動的生成 18 個・factory 経由 17 個を解決） | **どのボタンにも反応する処理が存在する（未結線ゼロ）** |
| R スクリプト | 現用 17 本（本解析 4・統合 1・補助 12）＋死蔵 34 本 | 全 51 本が構文 OK。現用 17 本は全行を精査 |
| 自動テスト | 既存 1,756 件 ＋ E2E 19 件 | 全て緑（環境変数を正しく与えた場合）。修正後は 1,770 件緑 |
| 指摘 | 207 件を検出 → **反証審査 79 件（全件審査完了）** | S1 5・S2 43・S3 20・S4 8・情報 3（うち誤検出 4） |

### 最重要 10 件

| # | 重大 | 何が起きるか | 分類 |
|---|---|---|---|
| 1 | S1 | **キャリブレーション表で「使わない」に外した点が、補正計算に混ざったまま**。実測で m/z 補正量が 0.042 Da ずれ、これは照合許容値 0.01 Da の**4 倍**。化合物同定が総崩れしうる | ③ |
| 2 | S1 | **クラスタを選んで再解析すると、選んだスポットの約半分が黙って捨てられる**（背景除去の二重適用）。信号の弱い組織ほど消える | ③ |
| 3 | S1 | 複数サンプル DESI の RPCA だけ、**補正後の値でマーカー検定**している（TIMS は正しい）。他手法と土台が違う | ③ |
| 4 | S1 | **「補正なし」を選んでもバッチ補正が実行され**、自動生成の Methods 文には「補正しなかった」と**誤記**される | ③ |
| 5 | S1 | 「PCA（無補正）」として表示されるクラスタが、実は **Harmony 由来のクラスタ**（独立した結果に見えるが違う） | ③ |
| 6 | S2 | **ログインしてもヘッダーの解析者名が常に空欄**。誰として作業中か画面から分からない（共有サーバでは別人のまま気づけない） | ① |
| 7 | S2 | サブプロジェクト B を開いても**A のデータフォルダが残り**、そのまま実行すると A のデータを B の解析として記録する | ② |
| 8 | S2 | **見出しを畳んだ状態で色や名前を変えて開き直すと、図が古いまま**。過去（ver51.9）の修正が実は空振りだったことも判明 | ② |
| 9 | S2 | **クラスタ絞り込み後の「元の UMAP への貼り戻し」が DESI・TIMS とも成立しない**（DESI は必ずエラー終了、TIMS は無言で何もせず完走） | ③ |
| 10 | S2 | 再解析画面の**切片フィルタ・サンプル選択チェックが読まれない**。外したはずのデータが解析に入る | ③ |

### ②「ボタンが違う動作をする／反映されない」の原因の総括

過去に体験された症状は、次の 4 つの型に整理できた。いずれも**エラーを出さずに失敗する**共通点がある。

1. **「変更しない」を返してしまう型**（最多）— 内部で「値が無ければ画面を更新しない」という書き方をしているため、**前の値が residual として残る**。サブプロジェクト切替のフォルダ残留（#7）、プリセット読込が効かない、が該当。
2. **後から自動処理が上書きする型** — 利用者の操作で正しい値が入った直後に、別の自動切替処理が既定値で塗り潰す。プリセットの付加イオン設定・再解析のイオンモードなどが該当。
3. **画面と計算で別の値を見る型** — 表示名で照合すべきところを内部 ID で比べている等。クラスタ名を変えると統計表示が「0 pixels」になる、が該当。
4. **宣言した相手が存在しない型** — 削除済みの画面部品に書き込もうとして、その処理全体が停止する。解析者名が空欄（#6）が該当し、**過去にも同型で「参照」ボタン 35 個が一斉に無反応**になった事故がある。

---

## 2. 調査方法

### 2.1 「読んだ」だけで終わらせない

コード読解だけでは、このアプリで過去に起きた不具合（宣言はあるが実体がない、修正したつもりが空振り）は捕まえられない。そこで**実際に動かす**ことを軸に据えた。

1. **アプリを 2 通りで起動**：認証設定を与えない場合（①-1 の再現）と、正しく与えた場合（探査用）。
2. **稼働中のアプリから「登録済みコールバック一覧」を採取**（381 件）。以後すべての静的解析はこの実データと突合し、「コードには書いてあるが実際には登録されていない」を検出できるようにした。
3. **実ブラウザ（Chromium）で操作**：ログイン → 全ページ巡回 → タブ切替 → 見出しの開閉 → ボタン押下、を自動実行し、ブラウザ側のエラー・サーバ応答を全件記録。
4. **疑わしい挙動は最小再現アプリを書いて実測**：例えば「削除ボタンの二重処理」疑惑は、同じ構造の小さなアプリを作り実ブラウザで 5 回試行して**誤検出と判明**した（§4.4）。
5. **統計の主張は数値で例証**：BH 補正・Otsu 閾値・平滑化の近傍探索などを numpy／base R で再現し、影響の大きさを数値で示した。

### 2.2 多段の分業と「反証させる」体制

- **第 1 段（発見）**：27 の担当に分けて全数監査。コールバックはモジュール別 11 群＋多重書込み＋ブラウザ側、R は現用 17 本＋統計手法＋設定の受け渡し経路。
- **第 2 段（反証）**：第 1 段の指摘を**別の検証官**に渡し、「間違いだと示せ」と指示。他所のガード・後段での上書き・到達しない分岐・既定値・ライブラリの実挙動を探させ、崩せなかったものだけを「確定」とした。
- 結果、**誤検出 1 件を摘出**（§4.4）、**重大度の過大評価 15 件を格下げ**した。**この報告書に載る「確定」は、いずれも一度は反証を試みて生き残ったものである。**

### 2.3 環境と再現性

- 元リポジトリは**一切変更せず**、複製に対して実行した（アプリが実行時にリポジトリ内へ書き込む設計のため）。
- Python 3.11／dash 2.18.2／R 4.3.3（base のみ）。**Seurat は導入していない**ため、R 解析の実行そのものは行っていない（§8 の制約）。

---

## 3. ① アプリ実行時のエラー

### 3.1 総括：サーバ側は健全

| 確認項目 | 結果 |
|---|---|
| HTTP 4xx / 5xx | **0 件** |
| コールバックのエラー応答 | **0 件** |
| 未捕捉の例外（page error） | **0 件** |
| 起動シーケンス | 正常（コールバック 381 件を登録、`/healthz` 緑） |
| R スクリプトの構文 | 現用 17 本・死蔵 34 本すべて OK |
| 既存テスト | 1,756 件すべて緑 ＋ E2E 19 件緑 |

**「アプリ実行時にエラーがないか」への回答は「サーバ側にエラーはない」である。** ただし以下 3 点は実行時の問題として確定した。

### 3.2 [S1・実行確認] Docker を使わない起動では、認証設定の保存先が固定パスで壊れる

認証情報の保存先が `/app/Data/Other/common/auth.json` という **Docker コンテナ内にしか存在しない住所**で決め打ちされている（`App/app/services/auth_service.py:25-33`）。環境変数 `AUTH_CONFIG_PATH` で上書きできるが、この変数は `.env.example`・`setup.sh`・`setup.bat`・`SETUP_GUIDE.html`・`DEPLOY.md` の**どこにも書かれていない**。

実測した挙動：

- **管理者権限で起動した場合**：ファイルシステムの最上位に `/app/` を勝手に作り、そこにパスワードのハッシュを書き込んだ（アプリは起動する）。意図しない場所に認証情報が残る。
- **一般ユーザー権限で起動した場合**：`PermissionError: '/app'` で**起動できない**。

さらに副次的な問題が 2 つある。(a) ファイルの親フォルダを作る前にロックを取ろうとする順序になっている（現行のロックライブラリが親フォルダを作るため今は表面化しない）。(b) `config.py:64` は `Data/Other/Common`（大文字 C）を使うのに認証は `common`（小文字）を使う。大文字小文字を区別する環境では別フォルダになり、`.gitignore` も小文字側を除外していないため、生成された認証ファイルが誤ってバージョン管理に入りうる。

**波及**：自動ブラウザテスト（E2E）の起動処理も同じ経路を通るため、この環境変数を与えないと E2E は「失敗」ではなく**「依存不足の可能性」として黙ってスキップ**される。つまり**テストが動いていないことに誰も気づけない**状態だった（正しく与えたところ 19 件すべて緑になった）。

### 3.3 [S2・実行確認] ログインしても解析者名が常に空欄

ヘッダーの「解析者: ○○ (tier)」を表示する処理が、**ver52.3 で画面から削除された部品**（`header_analyst_label_shared`）に書き込もうとしている（`App/app/callbacks/auth_callbacks.py:52-55`）。

実ブラウザでの実測：

```
header_analyst_label_landing  : 存在する / 中身は空文字 ""
header_analyst_label_analysis : 存在する / 中身は空文字 ""
header_analyst_label_shared   : 存在しない
コンソール: ReferenceError: A nonexistent object was used in an `Output` of a Dash callback.
            The id of this object is `header_analyst_label_shared` ...（ページ遷移のたびに発生）
```

存在しない 1 か所のせいで**処理全体が実行されず、実在する 2 か所にも名前が入らない**。裏側では解析者情報が正しく用意されているのに、表示段で止まっている。共有サーバで複数人が使う場合、**別人のアカウントのまま作業していても気づけない**。

これは「宣言した相手が存在しない → 例外も出ず機能だけが消える」という、このリポジトリで繰り返し起きている型である（同型で「参照」ボタン 35 個が一斉に無反応になった事故が ver42.1 にある）。**現在この型を検出するテストは存在しない。**

### 3.4 [S4・実行確認] もう 1 か所、同種の「存在しない部品」がある

`fs_umap_exclude_cluster` も同じエラーを出し続けている（`interactive_fullscreen.py:140` で全画面モーダルを開いたときだけ作られる部品を、常時参照している）。ただしこちらはモーダルを開けば実在するため、実害は「コンソールが恒常的にエラーで汚れ、本物のエラーが埋もれる」ことに留まる（§3.3 とは重大度が異なる）。

### 3.5 [S3・コード確認] 解析の起動に失敗すると「実行中」表示が残り続ける

解析開始時、記録簿に「実行中」と書いてから R を起動する（`analysis_runner.py:1029` → `:1102`）。R の起動自体に失敗した場合、記録簿を書き直す処理がないため、実際には何も動いていないのに「実行中」の表示が残る。次回アプリ再起動時の整合処理で解消されるが、それまでは誤った表示が続く。

### 3.6 [S3] 科学的に正しい統計機能が UI から呼び出せない

サンプル単位の検定（pseudobulk）、空間統計（Moran's I）、クラスタリング安定性評価が実装され、テストも通っているのに、**どの画面からも呼び出す経路がない**。しかも画面の注意書きは「群間の主張にはサンプル/ROI 集約（pseudobulk）をご利用ください」と**利用者に案内している**（`caveats.py:24`）。案内された機能が存在しない状態になっている。

---

## 4. ② ボタン・UI の結線

### 4.1 まず「良い知らせ」：ボタンの取り違えは 1 件もなかった

**214 個すべてのボタンについて、押下を受け取る処理が実在することを機械照合で確認した（未結線ゼロ）。** 「A を押したのに B の処理が呼ばれる」という配線間違いも、ボタン ID の取り違えや紛らわしい重複も**発見されなかった**。全ページが最初から描画される設計のため、「画面にない部品を指す」種類の事故も起きにくい。

したがって、ご経験された「押しても別のものが実行される」は、**配線が入れ替わっているのではなく、正しく呼ばれた処理が途中で黙って諦めている**か、**直後に別の処理が結果を塗り潰している**ために、そう見えていたと考えられる。以下がその実体である。

全 214 ボタンの対応表は**付録A**、全 353 コールバックの監査結果は**付録B**、各指摘の詳細（症状・根拠・再現手順・修正方針）は**付録C** にある。

### 4.2 型別に見た原因

#### 型 1：「変更しない」を返すため、前の値が残る

**［S2・実行確認］サブプロジェクトを切り替えてもフォルダが前のまま**（`project_callbacks.py:692`）

サブプロジェクト B の「解析」を押すと、B に保存された設定がない項目については「画面を変更しない」という指示が返る。その結果、直前に開いていた A のデータフォルダ・出力先が**そのまま残る**。画面のどこにも「今どのサブプロジェクトを操作中か」の表示がないため、気づく手段がない。そのまま実行すると **A のデータを解析し、A の出力先に書き、記録上は B の解析**になる。

同じ問題は ver52.3 に「インタラクティブ解析」側で修正済みで、コードにその旨のコメントまで残っている。**解析設定側にだけ同じ修正が適用されていない。**

実測（実ブラウザ）：A を開いた後に B を開くと、サーバ応答にフォルダ欄が含まれず（＝変更しない）、画面は A のパスのまま。

**［S2・実行確認］閾値に 0 を入れると既定値に化ける**（`analysis_callbacks.py:527`）

「log2FC 閾値」に **0**（＝倍率で絞り込まない、という正当な指定）を入れて実行すると、実際の計算には 0.25 が使われる。プログラム上「0 は値が無いのと同じ」と扱う書き方をしているため。画面は 0 のままなので、指定どおり解析されたと信じてしまう。同じ入力欄でも**再解析側は 0 を正しく通す**ため、経路によって解釈が違う。

#### 型 2：直後に自動処理が上書きする

**［S2・実行確認］プリセットを読み込んでも一部の設定が既定値に戻る**（`file_handlers.py:505` 他）

プリセットを読むと「✅ 読み込みました」と出るのに、**付加イオン（Adduct）フィルタだけは保存した組み合わせにならない**。プリセット読込が「イオンモード」を書き換えた瞬間、それに連動する自動切替処理が発火して既定の組み合わせで塗り潰すため。同型の上書きが「再解析のイオンモード・m/z 許容誤差」「サブプロジェクトのデータフォルダ」でも起きる。

**［S2・実行確認］キャリブレーションのプリセットが対応表に反映されない**（`analysis_callbacks.py:2110`）

プリセットを選ぶと成功メッセージは出るが、リファレンス/実測値の対応表がまったく変わらない（空のまま、または直前の内容のまま）。**プリセット保存機能が事実上使えない状態。**

#### 型 3：画面と計算で別の値を見る

**［S2・実行確認］クラスタに名前を付けると統計が「0 pixels」になる**（`interactive_cluster.py:99`）

クラスタを「Tumor」などに改名した後、クラスタ統計の表で行を選ぶと、右側の説明が「Tumor: 0 pixels (0.0%)」になる。表示名で照合すべきところを**内部のクラスタ番号と突き合わせている**ため、必ず一致しない。同じ画面の表には「100 pixels / 33.3%」と正しい数字が出るので、どちらが本当か分からなくなる。

#### 型 4：宣言した相手が存在しない

§3.3 の解析者ラベルが該当（詳細は §3.3）。

### 4.3 「変更が反映されない」の核心：見出しを畳むと図が古いまま

**［S2・実行確認］**（`interactive_umap.py:534`／`interactive_spatial.py:1114`）

UMAP や Spatial の見出し（アコーディオン）を**畳んだ状態**でクラスタ名の変更・色の変更・マージ表示の切替などを行い、そのあと見出しを開き直しても、**図が変更前のまま表示される**。設定欄は新しい値なのに図だけが古い、という食い違いが起きる。そのまま「一括保存」や PowerPoint 出力を行うと、**古い図が成果物として保存される**。

重要なのは、**この問題は ver51.9 で一度「修正済み」とされている**点である。今回の検証で、その修正は**挙動を何も変えていない空振り**だったことが判明した（見出しが閉じている間は状態が記録されないため、開き直したときに「変化なし」と誤判定される）。加えて UMAP 側では利用者ごとの識別が付いておらず、**複数人が同じ結果を開くと互いの状態を上書きし合う**。

### 4.4 疑ったが「白」だった項目（誤検出の公開）

監査が「疑いをすべて事実にしていない」ことを示すため、反証が成立した項目も記録する。

1. **削除ボタンの二重処理疑惑 → 白**。「削除確認を押すと 2 つの処理が同時に走り、片方が対象を消してしまうので削除が効かないことがある」と疑ったが、**同じ構造の最小アプリを作り実ブラウザで 5 回試行したところ、5 回とも正常に削除された**。Dash は片方が消す前の値をもう片方に渡す仕様のため、順序に関係なく成功する。ただしこれはライブラリの実装詳細に依存しており、将来の更新で崩れうる構造ではある（S4 の設計メモとして記録）。
2. **マーカー表の絞り込みが古い値のまま残る疑い → 白**（`interactive_loupe.py:359`）。ドロップダウン部品自身が「選択肢に無い値を自動で空に戻す」実装を持っており、実測でも残らなかった。
3. **解析者ラベル問題の「影響は軽微」という評価 → 誤り**。当初「本番では無症状」と評価した検証があったが、実ブラウザで**全ユーザーに常時見える形で機能が失われている**ことを確認し、評価を覆した（§3.3 が正しい）。
4. **軽量ビューアが 2 回構築される疑い → 白**（`lite_view_callbacks.py:242`）。前半（動的挿入で初回発火が抑止されない）は事実だが、後半の「全レポートが再構築される」は誤り。同じ結線を写した最小アプリで実測したところ、無害な通信が 1 回余分に走るだけで、描画は 1 回だった。
5. **解析手法の相互クリアで「どちらも未選択」に落ちる疑い → 白**（`file_handlers.py:39`）。壊れた設定を読ませても 8 回の実測すべてで片方が選択された状態に収束した。

### 4.5 ② の指摘一覧（50 件）

各行の詳細は**付録C-1**を参照。

| # | 重大 | 指摘 | 利用者から見た症状(要約) | 確証 | 修正 |
|---|---|---|---|---|---|
| 1 | S2 | sub_action_new_analysis: 前サブプロジェクトの設定値が残留 (ver52.3 同型の未修正残り) | サブプロジェクト B の「解析」ボタンを押して解析設定画面を開いたのに、データフォルダ・出力先フォルダの欄には直前に開いていたサブプロジェクト A の | 実行確認 | 機械的修正 |
| 2 | S2 | toggle_edit_sub_modal: confirm で無条件クローズ+フィールドクリア → name 空保存で | サブプロジェクトの編集画面でメモやフォルダを直し、ついでに名前を打ち直そうとして名前欄を空にしたまま「保存」を押すと、モーダルは普通に閉じてエラーも出 | 実行確認 | 機械的修正 |
| 3 | S2 |  | log2FC 閾値の欄に 0（＝フォールドチェンジで絞り込まない、という正当な指定）を入れて「解析実行」を押しても、実際の計算には 0.25 が使われ | 実行確認 | 機械的修正 |
| 4 | S2 | Spatial: 閉→再オープンで閉中の変更が反映されない (accordion noop ガードの記録欠落) | 「Spatial Mapping」の見出しを閉じた状態でクラスタの色を変えたり名前を付け替えたりして、あとで見出しを開き直すと、図が変更前のまま表示さ | 実行確認 | 機械的修正 |
| 5 | S2 | クラスタ情報: リネーム後に統計テーブル行選択で『0 pixels (0.0%)』誤表示 (表示名と raw ID の比 | クラスタに好きな名前を付けたあと、「クラスタ統計」の表でその行を選ぶと、右側の説明に「Tumor: 0 pixels (0.0%)」と表示され、サンプ | 実行確認 | 機械的修正 |
| 6 | S2 | relayoutフィルタが発火元でなく宣言順先頭のannotationsを採用し、別グラフの旧ドラッグ座標を誤保存 | 統合UMAPのクラスタ名ラベルを動かした後に、Spatial（またはサンプル別UMAP）のラベルを動かすと、動かしたはずのラベルが元の位置に戻ってしま | 実行確認 | 機械的修正 |
| 7 | S2 | 個体復元後の回転スライダ非同期により、フリップ1回で復元rotationが巻き戻り保存済み対応点がディスク上でも全消去さ | H&E の個体を切り替えたあと（またはページを開き直したあと）、回転スライダや左右／上下反転のチェックに一度触れただけで、その個体で苦労して打った位置 | 実行確認 | 機械的修正 |
| 8 | S2 | データ読込のたびに保存済み付加イオン選択が既定値で上書きされ、既定値が再保存される | キャリブレーションの「付加イオン」チェックを、例えば +H だけに絞って保存しても、そのプロジェクトを読み込み直すたびに必ず既定の4種（+H / +N | 実行確認 | 機械的修正 |
| 9 | S2 | キャリブレーション適用時、アノテーションファイル欄に指定した CSV が黙って無視される(要検証) | キャリブレーションの「アノテーションファイル」欄にファイル（同梱の MRM の .xlsx でも、TraceFinder / HMDB の CSV で | 実行確認 | 機械的修正 |
| 10 | S2 | Methodsモーダルの再施錠が不完全（閉→再開でパスワード無しに本文表示・DLボタンは有効表示のまま無反応） | Methods 文をパスワードで表示させた後、モーダルを閉じてもう一度「📝 Methods 文を表示」を押すと、パスワードを聞かれずに本文がそのまま出 | 実行確認 | 機械的修正 |
| 11 | S2 | アコーディオン再オープン時に再描画がスキップされ「閉じている間の変更が反映されない」— ver51.9 C-3 修正は挙 | UMAP や Spatial の見出しを畳んだ状態でクラスタ名の変更・色の変更・マージ表示の切替などを行い、そのあと見出しを開き直しても、図が変更前の | 実行確認 | 機械的修正 |
| 12 | S2 | UMAP 一括保存/サムネ登録: per_sample 表示中に保持 figure が追い出されていると空白 PNG を | サンプル別の UMAP を表示している状態で「一括保存」を押すと、真っ白(枠線だけ)の画像が UMAP_integrated.png という名前で Z | 実行確認 | 機械的修正 |
| 13 | S2 | RDS軽量化の進捗バーが実行中ずっと 0%（進捗 regex に re.MULTILINE 欠落） | 「RDS 軽量化」モーダルで実行を押すと、進捗バーが最後まで 0%「準備中」の帯のまま一切動かない。ログだけは流れていくので、固まったのか進んでいるの | 実行確認 | 機械的修正 |
| 14 | S2 |  | ログインしても、画面右上のヘッダーに出るはずの「解析者: 山田 (A)」という表示が最初から最後まで空欄のまま。誰としてログインしているのかが画面から | 実行確認 | 機械的修正 |
| 15 | S2 | プリセット読込の adduct_filter が auto_switch_adduct に即座に上書きされる | 保存しておいたプリセットを読み込むと、「✅ 読み込みました」と表示されるのに、付加イオン(Adduct)フィルターだけは保存した組み合わせにならず、イ | 実行確認 | 要設計判断 |
| 16 | S2 | reset_reanalysis_defaults が復元直後の再解析パラメータを既定値で上書き | サブプロジェクトの「解析」ボタンで前回の設定を開いたときや、プリセットを読み込んだときに、再解析の「イオンモード」と「m/z許容誤差」だけが保存した値 | 実行確認 | 要設計判断 |
| 17 | S2 | auto_switch_data_folder がサブプロジェクト復元/再解析転記直後の data_folder を既定 | プロジェクト一覧からサブプロジェクトの「解析」を開くと、そのサブプロに紐づけて保存してあったデータフォルダではなく、サイドバーの「既定データフォルダ」 | 実行確認 | 要設計判断 |
| 18 | S2 | データ読込復元の int_cal_adduct_filter が auto_switch_int_cal_adduct  | キャリブレーションの「付加イオン」チェックボックスを自分で絞り込んで保存しても、次にそのデータを読み込むと必ず初期状態(+H, +Na, +NH4,  | 実行確認 | 機械的修正 |
| 19 | S2 | キャリブレーションプリセット読込が switch_cal_sample の連鎖で旧テーブルに巻き戻される | キャリブレーションのプリセットを選ぶと「✓「〇〇」を読み込みました」と成功メッセージは出るのに、その下のリファレンス/実測値の対応表がまったく変わらな | 実行確認 | 機械的修正 |
| 20 | S2 | ランディングからの standalone 起動が auto_fill に乗っ取られ entry_mode が二重書込で  | 同じタブで一度でも解析を実行したあとにトップページの「インタラクティブ解析」を押すと、空の選択画面ではなく前回解析したプロジェクトが勝手に開き、プロジ | 実行確認 | 機械的修正 |
| 21 | S2 | 共有リンクが固定した result_dir を set_interactive_folders_from_sub_pro | 共有リンクを作った後にそのサブプロジェクトを解析し直すと、共有リンクを開いた人には「共有した時点の結果」ではなく「最新の結果」が表示される。画面上は正 | 実行確認 | 機械的修正 |
| 22 | S2 | 孤児 Output: header_analyst_label_shared (①-B 再確認・原因特定) | ログインしてもヘッダーの「解析者: 名前 (tier)」が常に空欄のままで、自分が誰としてログインしているのか画面上で確認できない。ページを移動するた | 実行確認 | 機械的修正 |
| 23 | S2 | 死にOutput: header_analyst_label_shared が layout に不在（既知候補①-B を※誤検出 | (C11-3 と同一の欠陥)ログインしてもヘッダーの「解析者: 名前 (tier)」が常に空欄で、誰として作業しているのか画面から分からない。C11- | 実行確認 | 機械的修正 |
| 24 | S3 | toggle_create_sub_modal: confirm で無条件クローズ → name 未入力時にサブプロジェ | 新規サブプロジェクト作成で「タイトル」を入れ忘れて「作成」を押すと、モーダルはすっと閉じるのに一覧にはサブプロジェクトが増えない。「タイトルを入れてく | 実行確認 | 機械的修正 |
| 25 | S3 |  | 「reduction を再利用して UMAP 以降だけ」を実行するボタン（④）を、UMAP の条件を変えずにもう一度押すと、前回と同じ名前のフォルダに | 実行確認 | 要設計判断 |
| 26 | S3 | 拡大対象が空のとき拡大ボタン押下でモーダルが開かないのに fullscreen_closed_trigger が加算され | DEG を計算していない状態で DEG の拡大ボタン（⤢）を押す、あるいは Feature Plot をまだ描いていない状態で Feature の拡大 | 実行確認 | 機械的修正 |
| 27 | S3 | standalone解析後の自動連携で entry_mode='sub_project' が無条件に書かれ、プロジェクト | プロジェクトに紐づけずに(ランディングの「インタラクティブ解析」から入って)フォルダを指定して解析し、その後インタラクティブ解析タブを開くと、それまで | 実行確認 | 機械的修正 |
| 28 | S3 | ブックマーク選択肢: _set_active_key 無しで label_from_active_state を呼び、S | Feature の検索欄には「760.58510 (PI 38:4)」のように化合物名が出るのに、その右隣の『ブックマーク』プルダウンだけは同じピーク | 実行確認 | 機械的修正 |
| 29 | S3 | 選択グループ undo がプロジェクト切替をまたいで残存し、旧プロジェクトの cell_ids を新プロジェクトへ保存し | プロジェクト A で選択グループを削除したあと、そのままブラウザで別プロジェクト B を開いて『削除を取り消す』を押すと、B に身に覚えのないグループ | 実行確認 | 機械的修正 |
| 30 | S3 | 既知候補②-C確定: interactive_umap.py:543 の session_id=None で _acco | 同じプロジェクトを複数の人（別々のブラウザ）が同時に開いているとき、後から見る人が UMAP の見出しを畳んだままプロジェクトを切り替え、そのあと U | 実行確認 | 機械的修正 |
| 31 | S3 | DEG 一括保存: 全クラスタ横断 Heatmap のファイル名に volcano の選択クラスタ suffix が付く | DEG の一括保存でダウンロードした ZIP の中で、ヒートマップの画像ファイル名が「Heatmap_Cluster3.png」のように特定クラスタの | 実行確認 | 機械的修正 |
| 32 | S3 | RDS軽量化: 一部ファイル失敗でも緑の『完了しました。』成功表示（parquet 側と非対称） | RDS 軽量化で一部の .rds が変換に失敗しても、画面には緑色で「完了しました。」と出て、実行結果サマリも緑色になる。よく見ると同じ緑の枠内に「エ | 実行確認 | 機械的修正 |
| 33 | S3 |  | データ読み込み中に「キャンセル」を押すと「キャンセルしています…」と出るが、押したタイミングが RDS 抽出の後(マーカー読み込み〜設定復元の段階)だ | 実行確認 | 機械的修正 |
| 34 | S3 | interactive_data_info への並行書込レース: reset と set_folders の後着勝ちで⚠ | サブプロジェクトのインタラクティブ解析を開いたとき、「⚠ 結果フォルダが見つかりません: …」という警告が出る回とまったく出ない回があります。出なかっ | 実行確認 | 要設計判断 |
| 35 | S3 | チェック全解除が「全選択」に反転する(annotation/ROIフィルタのNone集約) | 切片(Annotation)や ROI のチェックを全部外してから解析を実行すると、「1 つも選ばない=何も使わない」つもりなのに、逆に全部の切片/R | 実行確認 | 要設計判断 |
| 36 | S3 | (条件付) | 「出力先」の欄をうっかり空にしたまま解析実行を押すと、警告も確認も一切なく『解析を開始しました』と表示され、解析結果はアプリの作業フォルダの中（コンテ | 実行確認 | 機械的修正 |
| 37 | S3 | 色編集ロックが別クラスタ(先頭)に誤取得される (全 picker 書き戻し→triggered_id 取り違え) 要検(条件付) | 複数人で同じプロジェクトを同時に開いているとき、誰かがクラスタの色を『色見本（スウォッチ）』のクリックで変えると、他の人の画面では関係のない先頭クラス | 実行確認 | 機械的修正 |
| 38 | S3 | UMAP ポリゴン下書き overlay の Patch data[-1] 前提崩壊(空 figure への Patch(条件付) | UMAP をクリックして選択範囲の頂点を置いたあと、マーカーサイズや凡例などの表示設定を変えると、画面上のピンク色の下書き線だけが消える。それでも『下 | 実行確認 | 要設計判断 |
| 39 | S3 | PreFlight ポーリングが process ハンドル消失時に永久スピナー+無限ポーリングになる(条件付) | PreFlight 診断が実際には終わって結果ファイルもできているのに、画面は「PreFlight 診断を実行中です…」のスピナー表示のまま止まり、裏 | 実行確認 | 機械的修正 |
| 40 | S3 | annotation選択UIがTIMS追加データフォルダを無視する(条件付) | TIMS 解析で「追加データフォルダ」を足すと、そのフォルダにしか無いサンプルは切片(Annotation)選択の一覧に出てきません。その状態で主フォ | 実行確認 | 機械的修正 |
| 41 | S4 |  | データフォルダや出力先の指定が間違っている状態で「解析実行」を押すと、画面上部に赤い枠で「入力チェックでエラーが見つかりました: データフォルダ: フ | 実行確認 | 要設計判断 |
| 42 | S4 | プロジェクト切替時の _drop_state()/_set_active_key(None) が恒等 no-op（宣言さ | 利用者の画面には何も現れない。プロジェクトを切り替えても古い解析データがサーバのメモリに残り続けるだけで、表示や解析結果が間違うことはない(残ったデー | 実行確認 | 機械的修正 |
| 43 | S4 | Spatial/Feature 一括保存: サーバ保持 figure 不在時にボタンが無言で無反応(トースト無し) | 「📷 一括保存」を押しても、ダウンロードも始まらずエラーメッセージも出ず、まったく何も起こらないことがあります。同じ状態で「📌 サムネ登録」を押すと「 | 実行確認 | 機械的修正 |
| 44 | S4 | デフォルト適用ボタンが空欄を反映しない(`x or no_update` が空文字を飲む) | サイドバーの「DESI/TIMS 初期設定」「出力先の既定」の欄を空にした状態で「適用」を押すと、解析設定側の欄が何も変わりません。成功メッセージもエ | 実行確認 | 要設計判断 |
| 45 | S4 | _TAB_TO_PATH が実在タブ hne を欠き、docstring の /app/results・/app/his(条件付) | 「解剖×クラスタ (H&E)」タブを開いても、ブラウザのアドレスバーは直前に見ていたタブ(解析設定 または インタラクティブ解析)のアドレスを表示した | 実行確認 | 機械的修正 |
| 46 | S4 | 共発現散布図: 発現行列と plot_data の行順照合が無い（ver52.5 の番人が feature plot の(条件付) | 現状は利用者に見える症状は無い（共発現散布図は正しく描かれる）。将来なんらかの理由で発現行列とピクセル座標の並びが食い違った場合には、Feature  | 実行確認 | 機械的修正 |
| 47 | S4 | (条件付) | 利用者の画面には何も現れない。/app/interactive のようなアドレスを直接開くとプロジェクト一覧に戻される(これは意図した動作)が、その裏 | 実行確認 | 要設計判断 |
| 48 | INFO | マーカーテーブルのクラスタ filter が stale 値のまま全行除外され空表示になる※誤検出 | 利用者に見える不具合は無い。解析結果を切り替えると、クラスタ絞り込みは自動的に『全クラスタ』へ戻り、マーカーテーブルには新しいデータの全行が表示される | 実行確認 | 修正不要 |
| 49 | INFO | Lite ビュー初回表示で全レポートが必ず 2 回構築される(動的挿入 RadioItems の初期発火)※誤検出 | 利用者に見える変化はありません。軽量ビューアの表示は1回で完了し、スピナーが2度出ることも、描画が組み直されることもありません(内部で無害な通信が1回 | 実行確認 | 修正不要 |
| 50 | INFO | ※誤検出 | 利用者に見える異常は起きない。設定ファイルに DESI と TIMS の両方の解析手法が入っているような壊れたデータを読み込んでも、片方(DESI 側 | 実行確認 | 修正不要 |

---

## 5. ③ R スクリプト・解析内容の科学的妥当性

### 5.1 監査範囲

現用スクリプトは `config.py` が指す 17 本に確定した（それ以外の 34 本は死蔵で、アプリからは一度も参照されない）。

| 役割 | ファイル | 行数 |
|---|---|---|
| DESI 本解析 | `App/Script/DESI/260623_DESI-UMAP_Template_v16.R` | 3,015 |
| DESI 再解析（クラスタ絞り込み） | `App/Script/DESI/DESI_RDS_ClusterFilter_ver3.R` | 537 |
| TIMS 本解析 | `App/Script/TIMS/260623_DBSCAN_With_cluster_ver6_no-png_slim.R` | 2,814 |
| TIMS 再解析 | `App/Script/TIMS/260623_DBSCAN_ver18_Cluster_Filter_ReUMAP.R` | 1,444 |
| クラスタ統合 | `App/Script/Common/UMAP_Merge_Clusters_ver1.R` | 474 |
| 補助 | `App/Script/helpers/*.R` | 12 本 |

Python 側の統計実装（pseudobulk の Welch 検定＋BH、Moran's I、クラスタ安定性の ARI/silhouette）も、scipy／sklearn と数値照合して検証した。

### 5.2 【最重要】解析結果が実際に誤る問題（S1・5 件）

#### 5.2.1 キャリブレーションで「使わない」に外した点が補正計算に混ざる ［実行確認］

`App/app/services/analysis_runner.py:278`

除外の判定が「値が空かどうか」で書かれているが、実際に入る値は文字列の `"Yes"` / `"No"` である。プログラム上 **`"No"` という文字列は「空ではない」＝「使う」と判定される**ため、チェックを外した行も回帰計算に入る。

実測（4 点のうち 1 点を「使わない」にした表を渡した）：

| | 使用点数 | m/z 400 での補正量 |
|---|---|---|
| 現状（誤） | 4 点（除外されない） | **+0.04337 Da** |
| 正しい挙動 | 3 点 | +0.00120 Da |

差 **0.042 Da** は、化合物照合の既定許容値 0.01 Da の **4 倍**にあたる。

さらに悪いことに、実行時のサマリー表示は `"Yes"` の行だけを「使用ピーク」として出すため、**画面には出ていない点が計算に入っている**という、目視で気づけない構造になっている。補正係数は R 側で全特徴量の m/z 名を書き換え、名前が衝突した特徴量は統合されるため、影響は化合物名の間違いに留まらず**発現行列そのものの変質**に及びうる。

なお、**対話画面（インタラクティブ解析）側の同じ処理は正しく実装されている**（`interactive_calibration.py:1197`）。つまり「対話画面で妥当性を確認してから本解析」という運用をしていた場合、**確認した補正曲線と実際に適用された補正曲線が別物**になる。

→ 過去に「使わない」行を含む表でキャリブレーションを行った解析は、**係数の再検証が必要**。

#### 5.2.2 クラスタを選んで再解析すると、選んだスポットの約半分が消える ［実行確認］

`App/Script/DESI/DESI_RDS_ClusterFilter_ver3.R:275`

再解析は本解析スクリプトのコピーを**文章として書き換えて**実行する仕組みで、そのうち「背景除去はもう済んでいるので飛ばせ」という書き換え指示が、本解析側の記述変更に追従できず**空振り**している。結果、**すでに背景除去済みのデータにもう一度自動しきい値処理（Otsu 法）がかかる**。

Otsu 法は「与えられたデータを 2 グループに分ける閾値」を毎回計算し直すため、既にフィルタ済みのデータに再適用すると、**残った中でさらに信号の弱い側が切り捨てられる**。信号強度の低い組織（脂肪、壊死部など）は選択的に失われる。画面にもログにも警告は出ない。

#### 5.2.3 DESI の複数サンプル（RPCA）だけ、補正後の値でマーカー検定している ［実行確認］

`App/Script/DESI/260623_DESI-UMAP_Template_v16.R:2805, 2930`

サンプル間の位置合わせ用に加工した値（integrated アッセイ）を元に戻さないまま差次発現検定を実行している。Seurat の公式見解でも、統合後の補正値での検定は推奨されない（値が再構成されたもので、負値も取りうるため）。

**TIMS 側は同じ場面で正しく元の測定値に戻してから検定している**（`ver6:1850-1851`）。つまり DESI の RPCA 分岐だけが例外で、同じアプリの中で手法間の一貫性が崩れている。

#### 5.2.4 「補正なし」を選んでもバッチ補正が実行され、Methods 文が誤記される ［実行確認］

`App/Script/TIMS/260623_DBSCAN_With_cluster_ver6_no-png_slim.R:2539`

解析シナリオで「同一切片のクラスタ／群比較：補正なし」を選んでも、**Ctrl と KO を別ファイルで読み込んだ場合はファイル単位で Harmony（と RPCA）が実行される**。画面の既定表示もその補正済み結果になる。にもかかわらず、論文用に自動生成される Methods 文にはシナリオの文言が優先され「**バッチ補正は行わなかった**」と書かれる。

**実際の処理と論文の方法記載が食い違う**ため、そのまま投稿すると誤った記載になる。

#### 5.2.5 「PCA（無補正）」のクラスタが実は Harmony 由来 ［コード確認］

`App/Script/helpers/derive_uncorrected_pca.R:44-50`

結果画面で「PCA (uncorrected)」を選ぶと、Harmony とは独立した解析結果のように見える。しかし実際に表示されるクラスタ番号・各クラスタのマーカー一覧・空間マップ・GPT 解釈は、**すべて Harmony で決めたクラスタ**である（座標だけを無補正 PCA から作り直している）。「補正の有無でクラスタがどう変わるか」を見比べる目的には使えない。

### 5.3 設定が R に届かない問題（②の症状の R 側の実体）

再解析（クラスタ絞り込み）に集中している。**UI → 設定 → R 変数 → 実際の使用箇所**の全経路を追跡した結果：

| 設定 | 症状 | 該当 |
|---|---|---|
| 切片（Annotation）フィルタ | 再解析では**一度も R に渡らない**。外した切片が解析に入る | R13-04 |
| サンプル選択チェック | 再解析では**読まれない**。フォルダ全件が対象になる | R13-06 |
| UMAP・クラスタリング条件 | 再解析には**注入経路自体が無い**。PreFlight の推奨値を入れても効かない | R13-09 |
| 正規化 ON/OFF | DESI 再解析には**伝播経路が無く**、常に正規化 ON 相当になる（TIMS にはある） | R12-N3 / R13-08 |
| log2FC 閾値 | **DESI では検定に届かない**（図の色分けにしか使われない）。TIMS では効く | R11-N1 |
| m/z 許容誤差 | **DESI では 0.1 Da 固定**（UI 値は TIMS のみ） | R10-N7 |
| 正規化設定の保存 | 保存対象リストから漏れており、**再起動で必ず既定に戻る** | R13-02 |

### 5.4 クラスタ統合（マージ）機能が成立していない ［実行確認］

「クラスタを絞り込んで再解析し、その結果を元の UMAP に貼り戻す」機能について、**DESI・TIMS の両方で、別々の担当が独立に「構造的に成立しない」と結論した**。

- **DESI**：再解析で書き出すファイル名に `_KEEP_Cl_8` のような接尾辞が付くため、元データ側のピクセル名（`sample|101`）と再解析側（`sample_KEEP_Cl_8|101`）が**一致しない**。統合は最後の一歩で必ず失敗し、**解析全体が赤いエラーで終了**する（UMAP もクラスタも DEG も計算し終えた後に）。エラー扱いになるため結果フォルダがプロジェクトに自動登録されず、画面のプルダウンにも出てこない。
- **TIMS**：同じ接尾辞問題に加え、内部でピクセルの照合キーが**すべて NA（欠損）になる**書き方の誤りがある。こちらはエラーを出さずに完走し、ログには「保存しました」と出るが、**中身は何も貼り戻されていない**。

つまり **DESI は「必ず失敗する」、TIMS は「成功したように見えて何もしない」**。どちらも利用者は正しい結果を得られない。

### 5.5 統計手法そのものの評価

数値例証を伴って検証した結果、以下は**手法の選択として議論の余地がある**もので、バグではない。ただし現状の挙動が利用者の期待と食い違う点は指摘しておく。

- **多重検定補正（BH法）の対象範囲**：検定前に足切りされた特徴量が補正の分母に入らないため、補正後 p 値が本来より小さく（有意になりやすく）出る。加えて**既定の p 値閾値 0.05 のままだと、CSV に出る特徴量はすべて「有意」判定になる**ため、この設定は実質的に効いていない。
- **空間平滑化の効き方がデータ依存**：探索半径が座標の単位に対して固定値（0.1）のため、座標がピクセル番号や µm だと**平滑化は何もしない**（それでも記録上は「平滑化済み」になる）。0.05 mm 刻みなら 10〜12 近傍を平均する強い平滑化になる。**同じ設定でもデータセットによって前処理が別物**になる。
- **ピクセル＝独立標本の扱い**：現在の p 値はすべてピクセルを独立標本として計算している。コード自身は「探索的な順位付けであり群間推論ではない」と正直にラベル付けしており、正しい代替（サンプル単位の検定）も実装済みだが、**UI から呼び出せない**（§3.6）。

### 5.6 疑ったが「白」だった項目（誤検出の公開）

- **「正規化 OFF を選んでも log1p が黙ってかかる」→ 白**。画面の選択肢ラベルは「OFF（正規化済み入力: SCiLS RMS 等）」であり、その直下に「OFF 時の変換」を選ぶ欄（既定 log1p、「変換なし」も選択可）が常設されている。ツールチップの説明・記録への保存も整合しており、表記と実処理の乖離は無い。
- **設定注入の取り違え疑い 3 件 → 白**。同名の記述が複数行ある箇所で「間違った行を書き換えているのでは」と疑ったが、実行順を追跡した結果いずれも正しい行が書き換わっていた。
- **特徴量抽出の層選択（data と counts の取り違え）→ 白**。設計どおり正しい。

### 5.7 ③ の指摘一覧（29 件）

各行の詳細は**付録C-2**を参照。

| # | 重大 | 指摘 | 利用者から見た症状(要約) | 確証 | 修正 |
|---|---|---|---|---|---|
| 1 | S1 | patch_v8_disable_otsu の終了アンカー(seu_list[[ii]] <- filtering_re | クラスタを選んで「再解析」すると、選んだはずのスポットの半分ほどが黙って捨てられ、信号の強い側だけで UMAP とクラスタが作り直されます。特に信号が | 実行確認 | 機械的修正 |
| 2 | S1 | RPCA分岐は DefaultAssay を "integrated"(2805)に設定したまま FindAllMark | 複数サンプルの DESI 解析を回すと、RPCA フォルダに出る「クラスタごとの特徴分子リスト」(analysis_deg_all_markers.c | 実行確認 | 機械的修正 |
| 3 | S1 | キャリブレーション表で「使わない」にした行が本解析経路で除外されない（use="No" が truthy） | キャリブレーション表でチェックを外した参照ピーク（＝明らかにおかしいので使わないと決めた点）が、実際には補正曲線の計算に混ざったままになります。画面の | 実行確認 | 機械的修正 |
| 4 | S1 | 複数ファイル入力では『無補正』シナリオでも Harmony/RPCA が sample 単位で必ず実行され、Method | 解析シナリオで「同一切片のクラスタ／群比較（Ctrl vs KO 等）：補正なし＝無補正PCA」を選んでも、Ctrl と KO を別ファイルで読み込ん | 実行確認 | 要科学的判断 |
| 5 | S1 | 無補正 PCA companion が Harmony の入力 pca を流用すること自体は妥当(RunHarmony  | 結果画面の「統合手法」で「PCA (uncorrected)」を選ぶと、Harmony とは別の独立した解析結果のように見える。しかし実際に表示される | コード確認 | 要科学的判断 |
| 6 | S2 | p.adjust(deg$p_val,"BH")(2394,2710,2942)の適用集合は (i) min.pct/l | 解析設定タブの「p 値閾値」を既定の 0.05 のまま使うかぎり、この設定はまったく効かない。CSV に出てくる分子は全部が自動的に「有意」と判定され | 実行確認 | 要科学的判断 |
| 7 | S2 | 空間平滑化の radius=0.1/sigma=0.05 が座標単位に対してハードコードされており(App からの注入経 | 「空間平滑化」は必ず実行される設定になっていますが、実際にどれくらい効くかは読み込んだデータの座標の書き方次第です。座標がピクセル番号(1,2,3…) | 実行確認 | 要科学的判断 |
| 8 | S2 | DESI で FindAllMarkers の閾値が直書きされ、UI 設定(DEG_LOGFC_TH_VAL)が検定に届 | 解析設定タブで「log2FC 閾値」を変えて DESI 解析を回しても、出てくる特徴分子の一覧がまったく変わらない(設定が黙って無視される)。さらに自 | 実行確認 | 機械的修正 |
| 9 | S2 | KNOWN_DEAD とされる USE_ROI_AS_SAMPLE / ROI_FILTER について「ROI を使った | ROI(関心領域)ごとに別サンプルとして解析した DESI データで、クラスタを絞り込んで再解析すると、ROI に分けたサンプルが結果から丸ごと消える | 実行確認 | 要科学的判断 |
| 10 | S2 | resume_rds_paths が空だと RESUME_DIR_PATH が注入されず、テンプレート直書きの Wind | 「途中再開 (RDSから)」にチェックを入れてフォルダ欄を空のまま(または .rds が入っていないフォルダを指定して)実行すると、赤い入力エラーが表 | 実行確認 | 機械的修正 |
| 11 | S2 | DESI 再解析には入力正規化ポリシー (INPUT_NORMALIZED / NORM_MODE) の伝播経路が存在せ | DESI の本解析で「正規化 OFF(SCiLS RMS 等で正規化済み)」を選んで実行し、そのクラスタを絞り込んで再解析すると、再解析だけが勝手に「 | 実行確認 | 要科学的判断 |
| 12 | S2 | 再解析の Annotation（切片）フィルタが R に一度も注入されない完全な no-op | 再解析画面の「Annotation（切片）選択」でチェックを外しても、その切片のスポットが再解析にそのまま含まれます。画面上は正常に完了し、エラーも警 | 実行確認 | 機械的修正 |
| 13 | S2 | 再解析のサンプル選択チェックボックスが読まれず、フォルダ全件が対象になる | 再解析画面の「チェックを入れたサンプルが再解析対象になります」というチェックボックスが機能しません。特定のサンプルのチェックを外しても、そのサンプルが | 実行確認 | 機械的修正 |
| 14 | S2 | DESI 再解析は正規化ポリシーを継承せず、常に LogNormalize になる | R12-N3 と同一。DESI 本解析を「正規化 OFF」で回した後の再解析だけが黙って「正規化 ON」相当に戻る。画面にもログにも差異が出ない。 | 実行確認 | 要科学的判断 |
| 15 | S2 | 再解析には UMAP/クラスタリング ハイパーパラメータの注入経路が存在しない | PreFlight の「③ 推奨値を入力欄へ反映」で n.neighbors・dims を最適値に設定しても、再解析（クラスタ絞り込み）ではその値が一 | 実行確認 | 要科学的判断 |
| 16 | S2 | 単一サンプル実行(既定シナリオ)は公称 3000/30/30 でなく 1000HVG/20PC/20dims で走り、U | ファイルを1つだけ指定して TIMS 解析を実行すると、画面の dims 欄に 30 と入っていても実際には 20 次元で計算されます。29 や 31 | 実行確認 | 要科学的判断 |
| 17 | S2 | リトライ劣化(3000/30/30→1000/20/20→500/15/15)は発生してもログ以外どころかログにも残らな | 大きなデータでメモリが足りずに 1 回目の計算が失敗すると、アプリは黙って軽い設定（可変遺伝子 3000→1000→500、主成分 30→20→15、 | 実行確認 | 機械的修正 |
| 18 | S2 | ReUMAP置換: 無名ベクトルの文字添字で cell key が全NA化し、merge が NA 同士を総当り結合（O | TIMS で「抽出 (keep)」再解析をすると、抽出したクラスタを細かく分け直した結果を元の UMAP に貼り戻す機能が働きません。データが小さめの | 実行確認 | 機械的修正 |
| 19 | S2 | ReUMAP置換: rerun サンプル名の _KEEP_Cl_x suffix を吸収する .merge_sample | R6-N1 と同じ「抽出 (keep) 再解析の貼り戻しが効かない」症状のもう一つの原因です。仮に R6-N1 だけを直しても、元データ側のピクセル名 | 実行確認 | 機械的修正 |
| 20 | S2 | 選択DE(local): 選択と対象クラスタの重複ピクセルが無言で ident.2 側へ奪われる | インタラクティブ画面で UMAP を投げ縄選択し、Locally モードで「比較対象クラスタ」を選んで差次発現を実行したとき、選んだ範囲と対象クラスタ | 実行確認 | 要科学的判断 |
| 21 | S2 | DESI 再解析に正規化ポリシーが伝播せず、UI トグルが完全な死にコントロールになっている(条件付) | DESI の再解析画面には正規化の設定欄が最初から表示されない。本解析で「正規化 OFF」を選んでいても再解析はそれを引き継がず、黙って「正規化 ON | 実行確認 | 要科学的判断 |
| 22 | S2 | 正規化 ON/OFF と NORM_MODE が永続化されず、毎回テンプレ既定へ戻る(条件付) | 正規化のON/OFFと変換方法（log1p など）を手で選んでも、アプリを閉じて開き直すと元の既定に戻ってしまいます。さらに同じ画面のまま解析法のラジ | 実行確認 | 機械的修正 |
| 23 | S2 | セッション復元直後に ion_mode と adduct_filter が不整合になり、Negative でアノテーショ(条件付) | 前回 Negative（陰イオン）モードで解析して終わった後にアプリを開き直すと、イオンモードは Negative と正しく表示されているのに、その下 | 実行確認 | 機械的修正 |
| 24 | S2 | 取り込み RDS キャッシュの鍵が size+mtime のみで、ANNOTATION_FILTER / USE_EMB(条件付) | 解析設定タブから離れずに、切片(スライス)の選択だけを変えて「解析実行」をもう一度押すと、変更前のスライスのデータでそのまま解析が走ります。出力フォル | 実行確認 | 機械的修正 |
| 25 | S2 | keep モードのマージ統合が構造的に不成立: SAMPLE_NAME_MAP=NULL 固定 × rerun サンプル(条件付) | DESI で「抽出 (keep)」を選んだ再解析は、UMAP もクラスタリングも DEG も全部終わったあと、最後の一歩で必ず赤いエラーで止まります。 | 実行確認 | 機械的修正 |
| 26 | S3 | 『UI の正規化 OFF でも log1p が黙って適用され表記と実処理が乖離』という主張は不成立。OFF の選択肢ラベ(条件付) | 「正規化」を OFF にしたまま生データ(RMS 正規化していないデータ)を解析すると、UMAP のかたまりやクラスタの分かれ方が、組織の種類ではなく | 実行確認 | 機械的修正 |
| 27 | S3 | アノテーションの由来スイッチ（use_annotation_check）が永続化されず毎回 ["embedded"] に(条件付) | 「代謝物データベース CSV で m/z 照合する」をONにして解析した後、アプリを開き直すと、CSVのパス欄だけが前回のまま残り、照合のスイッチだけ | 実行確認 | 機械的修正 |
| 28 | S3 | TIMS 再解析で m/z アライメント・埋め込み化合物名・切片フィルタ・サンプル別校正がテンプレ既定へ戻る(条件付) | 1 回目の解析で「m/z アライメント (ppm)」を設定していても、再解析ではその設定が無かったことにされます（画面に再解析用の入力欄も無いため確認 | 実行確認 | 要科学的判断 |
| 29 | S4 | INPUT_NORMALIZED=TRUE 経路が spot 単位スケーリングを一切行わない(log1p/sqrt/no(条件付) | scale.factor そのものによる目に見える不具合は無い(値を変えてもクラスタも p 値も変わらないことを実測で確認した)。残るのは、正規化 O | 実行確認 | 機械的修正 |

---

## 6. テスト・検証の生結果

### 6.1 既存テストの実行結果（基線）

```
$ python3 -m pytest tests --ignore=tests/e2e -q
1756 passed, 1 skipped, 2 xfailed  (85.96s)

$ python3 -m pytest tests/e2e -q          # AUTH_CONFIG_PATH を与えた場合
19 passed, 2 skipped  (135.44s)           # skip 2 件は実データが必要なもの
```

- **skip 1 件**の理由は `scipy` 未導入（本体の依存には無い）。
- **xfail 2 件**は既知未修正として台帳に登録済みの項目（DESI 再解析でアノテーション DB が渡らない件など）。
- **重要**：`AUTH_CONFIG_PATH` を与えないと E2E は「失敗」ではなく**スキップ**になる（§3.2）。従来この 19 件は実行されていなかった可能性が高い。

### 6.2 ガードテスト（過去のバグ型を見張るテスト）

`test_callback_wiring.py` / `test_no_undefined_names.py` / `test_declared_targets_exist.py` / `test_r_injection_completeness.py` / `test_r_patch_anchors.py` / `test_ver52_3_callbacks_actually_run.py` の 6 本を詳細実行 → **62 passed, 2 xfailed**。

これらは優れた仕組みだが、**今回見つかった型のうち次のものは検出できていなかった**：

| 見逃された型 | 理由 |
|---|---|
| ブラウザ側処理の書き込み先が実在しない（§3.3） | 既存の「宣言した対象の実在確認」がサーバ側処理のみを対象にしている |
| 「変更しない」を返して前の値が残る（§4.2 型 1） | 静的解析では「意図した挙動」と区別がつかない |
| 直後の自動処理による上書き（§4.2 型 2） | 個々のコールバックは正しく、組み合わせでのみ発現する |
| 見出しを畳んだ間の変更が反映されない（§4.3） | 状態遷移をまたぐため、単発の呼び出しテストでは出ない |

### 6.3 R スクリプトの構文チェックと設定注入の機械検証

- 現用 17 本・死蔵 34 本すべて構文 OK（`Rscript -e 'parse(file=...)'`）。
- 設定注入（Python → R の 124 箇所）を実際の正規表現で照合 → **新規の空振りはゼロ**。既知の 3 件（台帳登録済み）のみ。
- 再解析の書き換え指示（R → R の 11 箇所）を照合 → **3 件が空振り**（うち 1 件が §5.2.2 の Otsu 二重適用）。

### 6.4 動的探査（実ブラウザ）

ログイン → 全ページ巡回 → タブ切替 → 見出し開閉 → ボタン押下を自動実行。

- サーバ側エラー（4xx/5xx）**0 件**、コールバックのエラー応答 **0 件**、未捕捉例外 **0 件**。
- ブラウザ側エラー：**ページ遷移のたびに 2 種類の「存在しない部品」エラー**（§3.3・§3.4）。
- 解析者ラベルが空であることを DOM から直接確認。

---

## 7. 修正の選択肢（ここからは承認が必要）

指摘を「直しても過去の結果が変わらないもの」と「変わるもの」に厳密に分けた。**§7.1 のみ本調査の承認範囲（明白なバグ修正）に該当する。**

### 7.1 Tier 1：数値が変わらない機械的修正（該当 43 件・**完了**）

これらは**修正しても過去の解析結果の数値は 1 つも変わらない**。UI の動作が仕様どおりになるだけである。各修正には「そのバグを検出する失敗テスト」を先に追加し、**修正前にそのテストが落ちることを毎回確認**してから直した。

| | 件数 |
|---|---|
| Tier 1 該当（付録の「修正区分 = 機械的修正・数値不変」を機械集計） | **43** |
| ver56.4 で実施 | 2 |
| ver56.5 で実施 | 32 |
| ver56.6 で実施 | 9 |
| **未着手** | **0** |

> **［訂正］** ver56.5 時点の本節には「**残りは深刻度 S4 の整理項目のみ**」と書いていたが、これは**誤りだった**。実際にはこの時点で利用者に見える誤動作（S2 1 件・S3 5 件）が残っていた。また「26 件実施済み」という表記は、下表の 1 行が複数の指摘をまとめていたための**過小表示**だった（実数 34）。ver56.6 で残り 9 件を実施し、コードを 1 件ずつ読んで未修正が無いことを確認したうえで、この表を事実に合わせて書き直した。

**ver56.4 で実施済み（2 件）**：

| 項目 | 内容 | 検証 |
|---|---|---|
| 認証設定パスの可搬化（§3.2） | 既定パスを `config.DATA_DIR` 由来へ。**Docker では従来と完全に同一パスに解決される**ため既存パスワードは失われない。手順書への記載・`.gitignore` 追加・フォルダ作成順の是正も同時に実施 | 新規テスト 7 件緑 |
| 解析者ラベルの復旧（§3.3） | 存在しない書き込み先を削除。**この型を検出する番人を新設**（動的生成 id を自動収集する方式で、幽霊エントリが溜まらない設計） | 実ブラウザで表示を確認・新規テスト 6 件緑 |

**ver56.5 で実施済み（24 件）**：

いずれも「そのバグを検出する失敗テストを先に追加 → 修正 → 全スイート緑」の順で行い、
**修正前に該当テストが落ちることを毎回確認**している。

| 項目 | 内容 | 検証 |
|---|---|---|
| サブプロジェクトの設定残留（§4.2） | `x or no_update` を明示値へ。既定値は `PARAM_BOUNDS` を単一の出どころとする | 新規 14 件 |
| モーダルの無音破棄（§4.2） | 検証に失敗したら閉じずにエラーを出す | 上に含む |
| 見出しを畳んだ間の変更（§4.3） | 閉じた状態も記録する／早期 return の順序是正／利用者ごとの識別を付ける | 新規 8 件 |
| クラスタ名変更時の統計表示（§4.2 型 3） | 行に内部 ID を持たせ、改名の衝突でも取り違えない | 新規 7 件 |
| 閾値 0 の扱い（§4.2） | 「0 は未入力ではない」と判定。同型だった m/z アライメントも同時に是正 | 新規 8 件 |
| RDS 軽量化の進捗バーが 0% 固定 | 進捗の読み取り指定を複数行対応へ。部分失敗を成功色で出さない | 新規 11 件 |
| H&E の対応点消失 | 復元時に回転角・反転も戻す | 新規 5 件 |
| キャリブレーションの付加イオン既定戻り | 復元中は自動切替を抑止する | 新規 4 件 |
| 共有リンクが最新結果を指す | 共有時は結果フォルダを固定する | 新規 5 件 |
| Methods モーダルの再施錠 | 閉じたら施錠状態へ戻す | 新規 4 件 |
| 解析起動失敗時の「実行中」固着（§3.5） | 失敗時に記録を書き直す。途中再開の空振りも是正 | 新規 4 件 |
| ラベル位置の誤保存 | **発火元のグラフの座標だけ**を使う。パン／ズームで他図の残骸を拾わない | 新規 3 件（実ブラウザ） |
| イオンモード↔付加イオンの不整合 | 既定値の出どころを `config.adducts_for_ion_mode()` に集約 | 新規 10 件 |
| 設定が既定へ戻る（永続化漏れ 14 項目） | 保存側・ホワイトリスト・復元側の 3 集合を機械的に突き合わせる番人つき | 上に含む |
| 一括保存の白紙 PNG／サムネの白紙上書き | 中身の無い図を 3 か所で弾く | 新規 14 件 |
| DEG のファイル名が別クラスタ | ヒートマップは自身のフォーカスクラスタで命名 | 上に含む |
| 一括保存が無言で終わる | 保存対象が無い理由をトーストで出す | 上に含む |
| standalone 起動の乗っ取り | 明示的に開いた画面には自動補完が手を出さない | 新規 8 件 |
| プロジェクト未連携で選択欄が消える | プロジェクトが決まったときだけモードを名乗る | 上に含む |
| アノテーションファイルの形式無視 | MRM で読めなければ TraceFinder／HMDB として読み直す | 新規 8 件 |
| リトライ劣化が記録されない | 採用した段と実効値をログ・サイドカー・受領書・Methods へ流す（**計算は不変**） | 新規 11 件 |
| 空振りしていた R スクリプト書き換えの削除 | 本体テンプレが同じことをするようになったため削除（番人が検出） | 上に含む |
| 追加フォルダの切片が選べない | 全フォルダを探すパス解決を追加 | 新規 12 件 |
| 拡大ボタンの裏で全再描画／診断の永久ポーリング | `no_update` へ是正／追えなくなったら理由を出して止める | 上に含む |

**ver56.6 で実施済み（9 件・これで Tier 1 は完了）**：

| 項目 | 重大度 | 内容 | 検証 |
|---|---|---|---|
| キャリブレーションのプリセットが旧テーブルに巻き戻る | **S2** | 読込側がサンプルセレクタの「切替前の値」も揃え、切替側は切替が起きていないなら何も書き換えない。切替前の表は写し（DataTable）ではなく Store から読む | 新規 7 件 |
| ブックマークの化合物名が常に欠落 | S3 | 名前を解決する前に、見ているプロジェクトを指し示す | 新規 15 件 |
| 選択グループの取り消しがプロジェクトを跨ぐ | S3 | 切替時に取り消しの控えを必ず捨てる | 上に含む |
| 読み込みキャンセルが後着の応答で上書きされる | S3 | 合図の識別子を最終段まで引き継ぎ、各段の冒頭で確認して打ち切る | 上に含む |
| 出力先が空欄でも解析を始められる | S3 | `Path("")` は「.」になる。空欄／空白のみ／未設定を等しく扱い実行前に止める | 上に含む |
| クラスタ色の編集ロックが別クラスタに掛かる | S3 | 色見本も発火元として拾う | 上に含む |
| プロジェクト切替の「state 破棄」が死にコード | S4 | 破棄対象を明示して渡し、宣言どおりに動かす | 新規 11 件 |
| 共発現散布図に発現量とスポットの対応検査が無い | S4 | Feature plot と同じ検査を呼ぶ（一致・判定不能は従来どおり通す） | 上に含む |
| タブとアドレスの対応表に H&E が無い／説明文が実装と食い違う | S4 | 1 行追加。説明文を実装（開き直すとトップへ戻る仕様）に合わせる | 上に含む |

**実行結果（ver56.6 時点）**: ユニット **1,936 件緑**（基線 1,756 → 新規番人 21 ファイル・約 180 件を追加）、
E2E **22 件緑**（2 件は実データが要るため skip）、R 構文チェック OK。

**Tier 1 は残り 0 件**。以降に残るのは §7.2・§7.3・§7.5 の**すべてご判断を仰ぐもの**である。

### 7.2 Tier 2-A：明白なバグだが、直すと結果の数値が変わる（**ver57.5 で実施済み・11 件**）

**動作が仕様と違うことは確定している**が、修正すると過去の解析と数値が変わるため承認待ちにしていた区分。
**ご承認をいただき ver57.5 で 11 件すべてを実施した**。

実施前に ver57.4 時点のコードで全 11 件を再調査し、**11 件とも未修正のまま生存**していることを確認したうえで着手した
（別の検証官による反証審査で 9 件 UPHELD・2 件 WEAKENED。WEAKENED は「バグは成立するが被害範囲の記述が過大」という訂正で、
下表の「直すと何が変わるか」はその訂正を反映している）。

各修正は「そのバグを検出する失敗テストを先に追加 → **修正前にそれが落ちることを実証** → 修正 → 全スイート緑」の順で行った。

| # | 項目 | 直すと何が変わるか | 付録 |
|---|---|---|---|
| 1 | キャリブレーションの「使わない」（§5.2.1） | 補正係数が正しくなる。**過去に外れ値を外して実行した解析は結果が変わる**（現状が誤りなので、正しくなる） | C-2 #3 |
| 2 | 再解析の背景除去二重適用（§5.2.2） | 選択したスポットが正しく残る。**過去の再解析結果より対象スポットが増える** | C-2 #1 |
| 3 | DESI-RPCA の検定対象（§5.2.3） | マーカー一覧・p 値・fold change が変わる。TIMS と同じ土台になる | C-2 #2 |
| 4 | DESI の log2FC 閾値が検定に届かない（§5.3） | 画面の設定が効くようになり、DEG 表の行数が変わる。**既定値（0.25）のまま使っていた解析の数値は変わらない**（従来の直書き値と同じため） | C-2 #8 |
| 5 | 再解析の切片フィルタ（§5.3） | 外した切片が実際に除外される（現状は入っている） | C-2 #12 |
| 6 | 再解析のサンプル選択（§5.3） | 外したサンプルが実際に除外される | C-2 #13 |
| 7-9 | クラスタ統合の成立（§5.4・3 件） | 現在得られない結果が得られるようになる | C-2 #18・#19・#25 |
| 10 | 取り込みキャッシュの鍵（§5.3） | 設定を変えた再実行が、旧データではなく**指定どおりのデータ**で走る | C-2 #24 |
| 11 | 正規化の書き戻し（§5.3） | 手動で選んだ正規化 ON/OFF が残る。これまで書き戻されて二重正規化になっていた実行が、指定どおりに走る | C-2 #22 |

**実施内容の詳細**

| # | 直し方 | 併せて直したこと |
|---|---|---|
| 1 | 「使う/使わない」の判定を `_cal_row_is_used()` に集約し、対話画面側（`== "Yes"`）と同じ意味に揃えた | 真偽値で保存された表も読む。`use` キーが無い行は「使う」とする（従来は**全行が黙って捨てられて**いた） |
| 2 | 文字列手術をやめ、v16 に `SKIP_BACKGROUND_FILTER` を置いて `replace_assign_line`（0 件で停止＝fail-closed）で差し替える形にした | 番人テストの `KNOWN_DEAD_ANCHORS` から該当 2 件を外した。旧い置換文は ROI 分割で**上書き事故**になる形だったので、その持ち込みも回避 |
| 3 | Harmony 分岐・TIMS 本解析と同じ形で、検定前に測定値アッセイへ戻す | — |
| 4 | 3 か所すべてで `DEG_LOGFC_TH_VAL` を使う | テンプレート既定値 0.10 を画面既定値と同じ 0.25 に揃えた（値を注入しない経路で閾値が黙って下がるのを防ぐ）。**併せて `DEG_MIN_PCT_VAL <- 0.25` を新設**し、Methods 文に TIMS の既定 0.05 が書かれていた誤記を解消（値は従来と同じで計算は不変） |
| 5 | ver18 に `V13_ANNOTATION_FILTER` を追加し、既存の 4 段パターンで ver6 コピーへ流す | — |
| 6 | 本解析側と同じ形で `sample_names` と `original_input_paths` の**両方**を絞る | 片方だけでは名前と実ファイルが食い違う |
| 7-9 | ver18 の `.make_cell_key` に名前付けを追加（共通マージスクリプト側には元からあった）／ReUMAP 置換にも接尾辞の対応表を渡す／DESI も対応表を組み立てて渡す（従来は `NULL` 固定） | R を実行して「名前なしベクトルの文字添字は NA」「全 NA の merge は総当り」を実証し、テストに残した |
| 10 | 取り置きした中身を左右する設定の指紋を鍵に加え、一致しなければ読み直してログに出す | 切片の並べ替えだけでは指紋が変わらないよう `sort` してから連結（無意味な読み直しを避ける） |
| 11 | 直前に自動で入れた既定値を控え、現在値がその既定のままのときだけ切り替える | 既定の判定に TIMS 再解析を含めた（従来は `== "tims_v8"` だけを見ており、「再解析へ送る」で**生データ扱いになり ON へ化けて**いた） |

**［訂正］** この節の表は以前 7 行しかなく、§7.5 の「11 件」と対応が取れていなかった
（1 行が複数の指摘をまとめており、どの指摘が何件目か追えなかった）。
また「再解析の正規化継承」を Tier 2-A に挙げていたが、これは付録では**要科学的判断**（§7.3 側）に分類されている項目で、
Tier 2-A ではない。上表は付録の分類（`機械的修正・過去の結果と数値が変わる` の 11 件）と 1 対 1 に対応させ直したもの。

### 7.3 Tier 2-B：科学的な方針判断が必要（文献調査の結果と選択肢）

**この節は実装を伴わない**。6 つの論点について査読文献・一次資料を調べ、根拠のある推奨案を示す。
**根拠が得られなかった論点は「文献未確認」と明記**し、一般的な実務上の選択肢の提示に留めた
（無い根拠を有るように書かない）。出典は DOI を付す。

なお本調査の環境では一部の出版社サイト（PubMed / Oxford Academic / ACS / Bioconductor）へ
直接到達できなかった。到達できた一次資料（Seurat 本体のソース等）と検索結果の書誌情報で
確認できた範囲を示し、原文に当たれなかったものはその旨を書いた。

---

#### 論点 1: 多重検定補正の対象範囲 ★ 推奨あり

**現状**: `min.pct` と `logfc.threshold` で足切りした**通過集合にのみ** BH 補正を当てている。

**根拠 1（一次資料・原文確認済み）**: Seurat 本体のドキュメント（`man/FindMarkers.Rd`）は
補正方法についてこう書いている。

> "p-value adjustment is performed using bonferroni correction based on the total number of
> genes in the dataset."
> "**Other correction methods are not recommended, as Seurat pre-filters genes using the
> arguments above, reducing the number of tests performed.**"

つまり**開発元自身が「足切りをしているので Bonferroni 以外は推奨しない」と明言**している。
本アプリは足切りを掛けたうえで BH を当てており、この注意書きに真っ向から反する使い方になっている。
（出典: satijalab/seurat `man/FindMarkers.Rd`。同ファイルの `p_val_adj` の定義も
"Adjusted p-value, based on bonferroni correction using **all genes in the dataset**"）

**根拠 2（書誌確認）**: Bourgon R, Gentleman R, Huber W (2010)
"Independent filtering increases detection power for high-throughput experiments",
*PNAS* 107(21):9546–9551, **doi:10.1073/pnas.0914005107**。
二段階（フィルタ→検定）方式は、**フィルタ統計量が帰無仮説下で検定統計量と独立**であれば
type I error 制御を保ったまま検出力を上げられる。逆に、独立でない組み合わせでは
type I error 制御を失うと明記している。安全なのはクラスラベルを使わない
nonspecific filter（全体分散・全体平均など）である。

本アプリの `logfc.threshold`（群間の log2FC）と `min.pct`（群別の検出率）は**いずれも
群ラベルを使う**ため独立性が成り立たない。したがって「フィルタ通過集合にのみ BH」は
この論文が警告する型に該当する。

**根拠 3（一次資料・原文確認済み）**: 同じ Seurat のドキュメントは、より根本的な注意も書いている。

> "p-values should be interpreted cautiously, as the genes used for clustering are the same
> genes tested for differential expression."

クラスタを作るのに使ったデータで、そのクラスタ間の差を検定している（いわゆる二度漬け）。
この問題については Gao LL, Bien J, Witten D (2024) "Selective inference for hierarchical
clustering", *JASA* 119(545):332–342, **doi:10.1080/01621459.2022.2116331** が、
クラスタで群を定義してから通常の検定を当てると**type I error が極端に膨らむ**ことを示している
（書誌確認。原文は未到達）。

**推奨**: 次のいずれか。**(a) を推奨する**。

| 案 | 内容 | 影響 |
|---|---|---|
| **(a)** | 検定は足切りなし（`min.pct=0, logfc.threshold=0`）で全特徴量に対して行い、BH は**全検定数**で当てる。表示・書き出しの段階で fold change による絞り込みを掛ける | 計算時間は増えるが、補正の分母が正しくなる。**有意と判定される行は現状より減る** |
| (b) | 足切りは残し、補正を Seurat 既定の Bonferroni（分母＝全遺伝子数）に戻す | 開発元の推奨どおりになる。BH より保守的なので有意行はさらに減る |
| (c) | 現状のまま、出力と Methods に「補正の分母は足切り通過数である」と明記する | 数値は変わらないが、読者が保守性を判断できる |

いずれの案でも、二度漬けの問題（根拠 3）は残る。**p 値は「順位付けの道具」であって
「有意性の証明」ではない**という位置づけを Methods に明記することを併せて推奨する。

---

#### 論点 2: 空間平滑化の半径指定 ─ 文献未確認（部分的根拠あり）

**現状**: 平滑化半径が `0.1` のハードコードで、座標の単位に依存する。整数格子の座標では
実質 no-op、物理単位（mm 等）の座標では強い平滑化になり得る。

**部分的根拠（書誌確認）**: MSI で空間情報を前処理／クラスタリングに取り込む手法は確立している。
- Alexandrov T ら (2010) "Spatial segmentation of imaging mass spectrometry data with
  edge-preserving image denoising and clustering", *J Proteome Res*,
  **doi:10.1021/pr100734z** — 各 m/z チャネルにエッジ保存型の平滑化を掛けてからクラスタリングする。
- Alexandrov T, Kobarg JH (2011) "Efficient spatial segmentation of large imaging mass
  spectrometry datasets with spatially aware clustering", *Bioinformatics* 27(13):i230–i238,
  **doi:10.1093/bioinformatics/btr246** — 近傍画素との距離を重み付けして空間を意識した
  クラスタリングを行う。

つまり「平滑化してからクラスタリングする」こと自体は MSI の標準的な作法であり、
問題は手法ではなく**半径の指定方法**にある。

**文献未確認**: 「半径を物理単位で指定すべきか画素単位で指定すべきか」を明示的に論じた
査読文献は、今回の調査では確認できなかった。上記 2 本は**画素格子（近傍画素）を単位に
議論しており**、物理単位での指定を推奨する記述は確認していない。

**推奨（実務上の判断として）**: 半径を**画素単位で指定**し、UI に露出する。
理由は (1) 上記 2 本を含む MSI の手法が画素近傍を単位にしていること、(2) 画素単位なら
「半径 1 = 隣接 1 画素」と意味が一意に決まり、座標系が変わっても挙動が変わらないこと。
最低限、**現在の半径が実際に何画素に相当したかを受領書と Methods に記録**することを推奨する
（現状は記録が無く、no-op だったのか強い平滑化だったのか後から判別できない）。

---

#### 論点 3: 「補正なし」シナリオでのバッチ補正 ★ 推奨あり

**現状**: 「補正なし」を選んでもファイル単位の Harmony が実行され、Methods には
「補正なし」と書かれる（§5.2.4）。

**根拠（書誌確認）**: Nygaard V, Rødland EA, Hovig E (2016)
"Methods that remove batch effects while retaining group differences may lead to exaggerated
confidence in downstream analyses", *Biostatistics* 17(1):29–39,
**doi:10.1093/biostatistics/kxv027**。
**群がバッチに偏って配置されている場合、実際の群差がバッチ差として現れ、バッチ補正は
群差を偏らせる（通常は縮める）** と述べている（原文は未到達。書誌情報と要旨で確認）。

MSI では 1 ファイル＝1 切片＝多くの場合 1 個体・1 条件であり、生物学的条件がファイル単位で
分かれる。この状況で**ファイル単位の Harmony を掛けることは、除きたい技術差ではなく
比較したい生物差を除く**ことに直結する。まさに上記論文が警告する構図である。

**推奨**: **実処理を選択に合わせる**（「補正なし」なら Harmony を実行しない）。
表示だけ直して実処理を残す案は、上記の理由から**推奨しない**。
条件がファイル単位で分かれる設計では、補正の有無は結果の解釈を変えるため、
Methods 文だけの修正では研究上の妥当性が担保できない。

---

#### 論点 4: 「PCA（無補正）」の表示 ─ 文献未確認

**現状**: 「PCA（無補正）」として表示している図のクラスタが Harmony 由来である（§5.2.5）。

**文献未確認**: 「補正前後の比較図でクラスタを揃えるか独立に取り直すか」を規定した
査読文献は、今回の調査では確認できなかった。実務ガイド（10x Genomics、
single-cell best practices 等）では「補正前後を batch とサンプル群で色分けして並べる」
「ASW / ARI / LISI / kBET などの定量指標で補正の質を測る」という記述が見られたが、
**査読済みの規範として引用できるものは確認していない**。

**推奨（実務上の判断として）**: 比較図の目的によって分かれる。

| 目的 | 妥当な作り方 |
|---|---|
| 補正が効いたかを見る | 座標は無補正、**色は同じクラスタラベル**（現状の作り）。ただし図の題と凡例に「クラスタは補正後の定義」と明記する |
| 無補正での構造を見る | 無補正の座標で**クラスタも取り直す**。補正後とはクラスタ数も対応も変わる |

現状は前者の作りなので、**最小の修正は表示名の是正**（例:「無補正 PCA 座標／補正後クラスタ」）である。
「PCA（無補正）」という名前だけを見ると後者だと誤解されるため、名前が実体と食い違っている点は
（統計手法の是非とは別に）表示の誤りとして扱ってよい。

---

#### 論点 5: Top5 マーカーの選抜基準 ★ 推奨あり

**現状**: 有意性のフィルタなしに fold change 順で上位 5 件を採る。

**根拠 1（一次資料・原文確認済み）**: 論点 1 に挙げた Seurat のドキュメントの注意書き
（クラスタを作ったデータで検定している）から、**p 値はここでは有意性の証明として使えない**。

**根拠 2（書誌確認）**: Bioconductor の OSCA（Orchestrating Single-Cell Analysis）は
マーカー検出について「p 値は**順位付けのためだけに**使う」「クラスタが経験的に定義され、
反復実験にわたって安定に再現できない以上、統計的有意性という概念に明確な意味は無い」
という趣旨を述べている（検索結果で確認。原文サイトへは未到達）。

**根拠 3（書誌確認）**: Pullin JM, McCarthy DJ (2024) "A comparison of marker gene selection
methods for single-cell RNA sequencing data", *Genome Biology* 25, Article 56 (2024),
**doi:10.1186/s13059-024-03183-0**。マーカー選抜手法 59 種を、実データ 14 セットと
シミュレーション 170 セット超で比較したベンチマークである
（原文は未到達。どの指標が最良かという具体的な結論は本調査では確認していない）。

**推奨**: **fold change 順という現状の方針は妥当**であり、変更を要しない。
p 値で絞る案はむしろ根拠が弱い（根拠 1・2 のとおり、この p 値には有意性の意味が無い）。
ただし次の 2 点を推奨する。

1. **図と表の見出しから「有意」の語を外す**（現状「上位マーカー」の意味で使われている箇所があれば）。
2. **効果量を併記する**（AUC や Cohen's d など、fold change 以外の効果量）。fold change は
   発現量が低い特徴量で不安定になりやすいため、`min.pct` 相当の検出率も同時に示すと
   読者が信頼度を判断できる。

---

#### 論点 6: p 値ゼロの床値処理 ─ 文献未確認

**現状**: アンダーフローで 0 になった p 値に人工的な床値を入れており、その値が CSV にも混入する。

**背景（技術的事実）**: 倍精度浮動小数点で表現できる最小の正の値はおよそ
`2.2e-308`（非正規化数を使ってもおよそ `1e-314`）で、それより小さい p 値は 0 に落ちる。
図では `-log10(p)` を取るため 0 のままでは描けず、床値を入れて最上部にプロットするのが
一般的な実装上の対処である。

**文献未確認**: 「床値を入れてよいか」「入れた値をどう表記すべきか」を規定した査読文献は、
今回の調査では確認できなかった。統計コミュニティでは「アンダーフローした p 値を
具体的な数値として報告するのは無意味（`9.9e-324` のような値は機械の下限を下回っている）」
という指摘が見られたが、**査読済みの規範として引用できるものは確認していない**。

**推奨（実務上の判断として）**: **図示用と出力用を分ける**。

- 図（Volcano 等）: 床値でプロットしてよい。ただし床に張り付いた点であることが分かる
  表示（例: 図の注に「p < 1e-300 は上端に描画」）を添える。
- CSV / 受領書: **床値ではなく実際の値（0 またはアンダーフローの明示）を出す**。
  例えば `p_val = 0` と `p_val_note = "underflow (< 2.2e-308)"` のように分けるか、
  `< 2.2e-308` という文字列で出す。人工値をそのまま数値列に入れると、
  受け取った側がそれを実測値として扱ってしまう。

---

#### まとめ

| 論点 | 判定 | 推奨 |
|---|---|---|
| 1. 多重検定補正の対象範囲 | **根拠あり・現状は非推奨の使い方** | 足切りなしで検定し全検定数で BH（案 a） |
| 2. 空間平滑化の半径 | 部分的根拠あり／単位の議論は文献未確認 | 画素単位で指定し UI へ露出。最低限、実効半径を記録 |
| 3. 「補正なし」の定義 | **根拠あり・現状は危険な構図** | 実処理を選択に合わせる（Harmony を実行しない） |
| 4. 「PCA（無補正）」の表示 | 文献未確認 | 表示名を実体に合わせる（最小修正） |
| 5. Top5 の選抜基準 | **根拠あり・現状は妥当** | 変更不要。効果量の併記を推奨 |
| 6. p 値ゼロの床値 | 文献未確認 | 図用と出力用を分ける |

**実装はいずれも行っていない**。1・3 は結果の数値が変わるため、ご承認をいただいてから着手したい。

### 7.4 対応不要（利用者判断）

- **スクリプト名「DBSCAN」と実際の手法（Leiden）の不一致**：ご指示により**変更しない**。実際のクラスタリング手法が Leiden であることは本報告書に記録する（自動生成の Methods 文は Leiden と正しく記載されており、誤記はない）。

### 7.5 いま残っているもの（全体像）

Tier 1（承認不要の機械的修正）に加え、ver56.7 で**「解析結果の数値は変わらないが、どう動くのが正しいかの判断が要る」6 件**も実施した。
その後、数値が変わる 11 件（ver57.5）と、科学的な方針判断が要る 10 件（ver58.0）も方針のご指定を受けて実施した。

| 区分 | 件数 | 内訳 | 状態 |
|---|---|---|---|
| ~~**§7.2 Tier 2-A**（機械的だが数値が変わる）~~ | ~~11~~ | ~~**S1 3**・S2 8~~ | **ver57.5 で実施済み**（§7.2） |
| ~~**§7.3 Tier 2-B**（科学的な方針判断）~~ | ~~12 件 / 6 論点~~ | ~~**S1 2**・S2 9・S3 1~~ | **ver58.0 で実施済み**（§7.8） |
| ~~**要設計判断のうち、数値が変わるもの**~~ | ~~4~~ | ~~S2 3・S3 1~~ | **ver58.1 で実施済み**（§7.9） |
| ~~要設計判断のうち、数値が変わらないもの~~ | ~~6~~ | — | **ver56.7 で実施済み**（§7.6） |

**デバッグ総点検の指摘は、これですべて処置済みになった。**
残るのは §7.7(b) の 7 件（別セッションが ver57.x で持ち込んだ問題）のみ。

#### S1（解析結果が実際に誤る）5 件 — **3 件は実施済み・2 件が残る**

| # | 内容 | 直すとどうなるか | 状態 |
|---|---|---|---|
| 1 | キャリブレーション表で「使わない」にした行が本解析で除外されない（§5.2.1） | 補正係数が正しくなる。**過去に外れ値を外して実行した解析は結果が変わる** | **ver57.5 で実施** |
| 2 | DESI 再解析で背景除去（Otsu）が二重に適用される（§5.2.2） | 選択したスポットが正しく残る。**過去の再解析より対象スポットが増える** | **ver57.5 で実施** |
| 3 | DESI-RPCA の検定が integrated assay 上で行われる（§5.2.3） | マーカー一覧・p 値・fold change が変わる。TIMS と同じ土台になる | **ver57.5 で実施** |
| 4 | 「補正なし」を選んでも Harmony が実行され、Methods には「補正なし」と書かれる（§5.2.4） | 実処理が選択どおりになる。**クラスタが変わる** | **ver58.0 で実施** |
| 5 | 「PCA（無補正）」として表示する図のクラスタが Harmony 由来（§5.2.5） | 無補正の空間でクラスタを取り直す方針を採ったため**数値が変わる** | **ver58.0 で実施** |

**S1 5 件はすべて実施済み**（1〜3 は ver57.5、4・5 は ver58.0）。

#### 要設計判断のうち、数値が変わる 4 件（**ver58.1 で実施済み**・§7.9）

いずれも「**復元した設定を、直後に走る自動処理が既定値で上書きする**」型である。
直すと設定が効くようになるため、**同じ設定で再実行した結果が変わる**。
そのため §7.2 と同じ扱いにし、S1 5 件の判断とまとめて決める（ご指示による）。

| 症状 | 重大度 |
|---|---|
| プリセットを読み込んでも付加イオンだけ既定に戻る | S2 |
| 再解析のイオンモード・m/z 許容誤差だけが既定に戻る | S2 |
| **サブプロジェクトを開くとデータフォルダが既定に戻る — 別の場所のデータを解析してしまう** | S2 |
| 切片/ROI のチェックを全部外すと、逆に全部が対象になる | S3 |

**直し方はもう分かっている**。この型は Tier 1 で 2 回直しており
（キャリブレーションの付加イオン／プリセットの較正表）、
「**復元を優先し、復元中は自動切替を抑止する**」という形がこのリポジトリに確立している。
→ ご承認をいただき、同じ形を当てて **ver58.1 で実施した**（§7.9）。

### 7.6 ver56.7：数値が変わらないが仕様の判断が要った 6 件（実施済み）

「解析結果の数値は変わらないが、どう動くのが正しいかを決める必要がある」ため
Tier 1 に入れていなかったもの。ご判断をいただいて実施した。

| 症状 | 重大度 | 決めた方針 | 検証 |
|---|---|---|---|
| ④「reduction を再利用」だけ確認なしで前回の結果を上書きして消す | S3 | **確認画面を出す**（他 2 ボタンと同じ）。出力先の決め方も 1 か所に集約し、確認した先と実際に書く先が食い違わないようにした | 新規 11 件 |
| 「⚠ 結果フォルダが見つかりません」が出る回と出ない回がある | S3 | 表示欄の持ち主を「フォルダを実際に調べている側」に一本化。**警告が毎回必ず出る** | 新規 13 件 |
| UMAP の下書き線が消えるのに「確定」でき、見えない範囲が選ばれる | S3 | 図を作り直す操作（表示モード・マージ・データ切替）で下書きを捨てる。ラベルも同時に消える | 上に含む |
| 入力チェックの赤いエラーと「解析を開始しました」の緑が同時に出る | S4 | **エラー時は実行を止める**。①④ でもチェックを走らせる。ただし「出力先の親フォルダが無い」は実行時に自動作成されるので**止める対象から外した**（外すと正常な実行まで止まる） | 新規 16 件 |
| 「適用」ボタンが空欄では無反応（成功もエラーも出ない） | S4 | 「空欄で既存の指定を潰さない」方針は維持し、何が起きたかを必ず伝える | 新規 9 件 |
| タブ復元の説明が実装と食い違う | S4 | 実害が無いので**説明のほうを実態に合わせた**（実装は据え置き） | 上に含む |

**実行結果（ver56.7 時点）**: ユニット **1,985 件緑**、E2E **22 件緑**（2 件は実データが要るため skip）。

### 7.7 ver57.5 の再調査で新たに分かったこと

Tier 2-A の実施にあたり、ver57.4 時点の全スクリプトを対象に現状を再調査した。
その過程で、**本報告書自身の誤り**と、**ver57.x で新たに持ち込まれた問題**が見つかったので記録する。

#### (a) 本報告書の誤り（訂正）

| # | 内容 | 対応 |
|---|---|---|
| 1 | §7.2 の表が 7 行しかなく、§7.5 の「11 件」と対応が取れていなかった | §7.2 を付録の 11 件と 1 対 1 に対応させ直した |
| 2 | §7.2 に「再解析の正規化継承」を挙げていたが、付録では**要科学的判断**（§7.3 側）に分類されている | §7.2 から外した |
| 3 | §7.3 は「6 論点」だが §7.5 は「12 件」としている。**論点と指摘の粒度が違う**ことを書いていなかった | 12 件の指摘が 6 論点に集約される関係であることを明記する（下記） |
| 4 | 付録が「1 件に統合して計上する」と明記している 2 件（DESI 再解析の正規化非伝播）を、§7.5 が別件として数えている | 12 件は**指摘の件数**であり、判断が必要な論点は 6 つ。件数と論点数を混同しない |
| 5 | BH 補正の指摘が DESI の 3 か所に限定されているが、**同型が TIMS 本解析と選択 DE にも存在する**（過小評価） | §7.3 論点 1 の対象は DESI に限らないことを明記する |
| 6 | 空間平滑化（§7.3 論点 2）の記述が DESI 限定であることを本文で明示していない（**TIMS は既定 OFF**） | 論点 2 は DESI の話であることを明記する |
| 7 | Tier 1「43 件」に、報告書自身が誤検出（OVERTURNED）と判定した項目が 1 件混入している | 43 という数字は「機械的修正に分類された件数」であり、うち 1 件は白判定として処置済み（実施ではない） |

**§7.3 の 12 件 ↔ 6 論点の対応**: 論点 1（BH 補正）＝ 1 件／論点 2（空間平滑化）＝ 1 件／
論点 3（「補正なし」でも Harmony）＝ 1 件／論点 4（PCA 無補正の表示）＝ 1 件／
残り 8 件は「再解析に設定が伝わらない」型（正規化の非伝播 3 件・UMAP ハイパラ・単一サンプルの次元数・
ROI 再解析の消失・選択 DE の重複ピクセル・TIMS 再解析の設定既定戻り）で、
**論点 3 と同じ「どこまで初回の条件を引き継ぐか」という方針判断**に帰着する。

#### (b) ver57.0〜57.4 で新たに持ち込まれた問題

別セッションが入れた変更（qs2 移行・解析状況レポート・RDS 軽量化の表示・圧縮スレッド数）を点検した。
**我々の 49 修正・報告書・付録との衝突は無く、RDS の読み書きに関する干渉も無い**。
ただし次が見つかった（いずれも本調査の Group① とは別件で、**未着手**）。

| # | 内容 | 重大度 |
|---|---|---|
| 1 | RDS 軽量化の増減表示 `.format_delta` が **NA で例外を投げ、軽量化を途中で落とす**（ver57.3 が持ち込んだ回帰） | S2 |
| 2 | 同じログの中で増減の符号の向きが逆（個別行は「縮小＝マイナス」、サマリは「削減率」） | S3 |
| 3 | 解析状況レポートが**エラー終了でも終了コード 0** を返し、PowerShell 側が緑で「正常に終了済み」と表示する | S2 |
| 4 | 状況判定の優先順位がアプリ本体と逆（status ファイルを PID 生存より優先）で、結論が食い違いうる | S3 |
| 5 | ローカル実行時に PID 名前空間が食い違い、走っている解析を「停止しています」と誤診しうる | S3 |
| 6 | `job_registry._pid_alive_windows` の説明と実装が逆（取得失敗時の倒し方）。ver56.6 で直した型の再発 | S4 |
| 7 | ver57.4 の是正が Python 側だけで、それを受ける PowerShell の結論行に旧い誘導が残っている | S4 |

### 7.8 ver58.0：科学的な方針判断が要った 10 件（実施済み）

§7.3 の 12 指摘 / 6 論点を、ver57.5 時点のコードに対して再調査し直したうえで
**A-1〜A-10 の 10 件**に整理し、方針のご指定を受けて実施した。**全件が解析結果の数値を変える。**
DESI に「バッチ補正の要否」を選ぶ欄を新設したので機能追加として +1.0 とした。

| # | 内容 | 決めた方針 | 数値への影響 |
|---|---|---|---|
| A-1 | 「補正なし」を選んでもバッチ補正が実行される（TIMS・DESI） | 補正の要否を明示の旗にする。**DESI にも選択欄を新設** | 複数ファイルの「補正なし」解析でクラスタが変わる |
| A-2 | 「PCA（無補正）」の図のクラスタが補正後のもの | **無補正の空間でクラスタを取り直す** | 無補正ビューの番号・マーカー・空間マップがすべて変わる |
| A-3 | 足切り後の集合にだけ多重比較補正を当てていた（5 か所） | 足切りなしで検定し**全検定数**で補正。CSV は従来どおり絞る | 有意と判定される行が減る |
| A-4 | DESI の空間平滑化がデータ依存で効く | 平滑化を行わない。**有無を記録に新設** | mm ピッチのデータでクラスタが変わる |
| A-5 | ROI 別サンプルの再解析でデータが丸ごと消える | 元 .txt へ逆引きし、ROI 分割をやり直す。失敗時は停止 | 消えていた ROI 別サンプルが現れる |
| A-6 | DESI 再解析に正規化ポリシーが届かない | 画面の値を渡す。使う値を再解析パネルに表示 | 二重正規化が止まりクラスタが変わる |
| A-7 | 再解析に UMAP 条件を渡す経路が無い | 画面の 4 条件を伝播（未指定ならテンプレ既定） | 再解析の UMAP とクラスタが変わる |
| A-8 | 単一サンプルで画面の次元数が効かない | 後方互換ガードを外す | 単一サンプルが 20 → 30 次元になる |
| A-9 | 範囲選択 DE で重なりが黙って相手側に取られる | **実行前にラジオで選ばせる**（既定＝除く）。件数を必ず表示 | 重なりのある比較の結果が変わる |
| A-10 | TIMS 再解析に m/z 条件・サンプル別キャリブレーションが届かない | 伝播する。共通の回帰式が無い場合の無補正も画面に明示 | 補正量が実際に掛かるようになる |

#### 実施の過程で分かったこと

1. **`min.pct=0` / `logfc.threshold=0` だけでは A-3 は成立しない。**
   Seurat の `FindAllMarkers` は `return.thresh = 1e-2` が既定で **p<0.01 の行しか返さない**。
   5 か所とも `return.thresh` を指定していなかったため、`return.thresh = 1` を明示しない限り
   **補正の分母は今までどおり**だった。これが最大の落とし穴だった。

2. **A-7 は当初案から範囲を縮小した。**
   `CLUSTER_K_PARAM` と解像度は**画面に対応する入力欄が存在しない**。受け手だけ作っても
   値を供給する側が永久に無く、「注入経路はあるが誰も使わない」空振りを増やすだけなので作らなかった。

3. **A-4 に Python 側の注入経路を足さなかった。**
   本解析と再解析は同じテンプレート実体を共有するため、テンプレート側の既定を変えるだけで
   両方に効く。注入を足すと 2 つの出典ができて食い違う余地が生まれる。この不変条件はテストで固定した。

4. **受領書の DE 閾値が実処理とずれていた（A-3 の取りこぼし）。**
   `provenance.ONTHEFLY_DE_FIXED_PARAMS` の `min_pct` は 0 に直したのに `logfc_threshold` が
   0.25 のまま残り、実処理は 0 なのに受領書だけが「|log2FC| ≥ 0.25 に限定して検定した」と
   主張していた。シグネチャの既定値と突き合わせる検査を新設し、片方だけ動かすと落ちるようにした。

5. **ガードテストが設計どおり機能した。**
   `test_r_injection_completeness.py` の `KNOWN_DEAD` に「キーを足した瞬間に無言で効かなくなる」と
   書かれていた 2 件（`USE_ROI_AS_SAMPLE` / `ROI_FILTER`）が、まさにその形で A-5 の実害になっていた。
   受け手を新設したことで `KNOWN_DEAD` は空になった。
   `test_settings_restore_round_trip.py`（復元するキーには保存経路が要る）と
   `test_numeric_input_bounds.py`（`x or 既定` を数値入力に使わない）も、実装中に 1 回ずつ発火した。

#### 検証

- ユニット全件 **2,308 件緑**（本作業で追加した回帰テスト 92 件を含む）
- 編集した R スクリプトはすべて `Rscript -e 'parse(...)'` を通した
- R の意味論に依存する箇所は **Rscript を実際に走らせて実証**した
  （設定注入の各型・ROI 名の逆引き・正規表現メタ文字のエスケープ）

### 7.9 ver58.1：復元を自動処理が上書きする 4 件（実施済み）

「バグと確定しているが、どう直すかに判断が要る」として最後まで残っていた 4 件。
方針のご指定を受けて実施した。**直すと設定が実際に効くようになるため、
同じ設定で再実行した結果が変わる。**

| # | 内容 | 決めた方針 | 数値への影響 |
|---|---|---|---|
| B-1 | プリセット読込で付加イオンだけ既定に戻る | 復元中は自動切替を抑止する | m/z 照合の対象が指定どおりになる |
| B-2 | 再解析のイオンモード・m/z 許容誤差だけ既定に戻る | 同上 | 再解析が保存時の条件で走る |
| B-3 | サブプロ復元・再解析転記でデータフォルダが既定に戻る | 同上 | **別の場所のデータを解析しなくなる** |
| B-4 | 切片/ROI を全部外すと逆に全部が対象になる | **実行前に止める**（ご指定） | 外したはずのデータが混ざらなくなる |

#### 実施の過程で分かったこと

1. **「同じ値を書くだけなら下流は発火しない」は成立しない。**
   実ブラウザ (Chromium) の最小再現で A/B を取った。ガード無しでは復元値
   `["+H","+Na"]` が `["-H"]` に塗り潰され、ガード有りでは残る。
   付録 #15 の実測と一致する。

2. **旗を降ろす経路が要る。** 復元中フラグを立てるだけだと、以後の**手動の**
   イオンモード変更や解析法の切替まで効かなくなる（復元を守るつもりで手動操作を殺す）。
   復元は 1 レスポンスで終わる一過性の出来事なので、次の周回で降ろす。

3. **B-4 は表示側と実行側の両方に同じ材料を渡す必要がある。**
   片方だけだと、ver56.7 で直した「赤いエラーと『解析を開始しました』の緑が
   同時に出る」型がそのまま再発する。

4. **止めすぎない線引き。** ROI フィルタは「ROI をサンプルとして扱う」ときにしか
   使われないので、OFF のときの全解除では止めない。

#### 検証

- ユニット全件 **2,339 件緑**（本作業で追加した回帰テスト 31 件を含む）
- E2E **22 件緑 / 2 skip**
- Dash の発火順に依存する箇所は、**実ブラウザの最小再現で A/B を取ってから**実装した

---

## 8. 本調査の制約

1. **R 解析の実行そのものは行っていない**。Seurat を含む R 解析環境の構築は本調査の範囲を超えるため、③の検証は「コードの精読 ＋ 数式・アルゴリズムの数値例証（numpy／base R）＋ 設定受け渡し経路の実測」で行った。**実データを流した結果の比較はしていない**。
2. **実データを搭載していない**。ログイン・画面遷移・ボタン押下・設定の受け渡しは実測できたが、「実際の MSI データを読み込んだ後の挙動」は検証できていない。§5 の各項目のうち、影響の**大きさ**（例：何割のスポットが失われるか）は実データでの確認が望ましい。
3. **重大度は運用実態に依存する**。例えば §5.2.1 は「キャリブレーション表で点を外す運用をしていたか」で影響が変わる。
4. **反証審査は全 79 件で完了した**（当初 12 件が未完了だったが、本報告書の確定時点までに終えた）。

---

## 9. 付録

| ファイル | 内容 |
|---|---|
| `DEBUG_AUDIT_2026-08_appendix_buttons.md` | **付録A**：全ボタン結線表（214 件）。ボタン → 呼ばれる処理 → 効果 → 判定 |
| `DEBUG_AUDIT_2026-08_appendix_callbacks.md` | **付録B**：全コールバック監査表（401 行）。発火条件 → 書き込み先 → 判定 |
| `DEBUG_AUDIT_2026-08_appendix_findings.md` | **付録C**：全指摘の詳細台帳（79 件）。症状・科学的影響・検証の経緯・再現手順・根拠・修正方針 |
